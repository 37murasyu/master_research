# 力学計算の再検算

検算日：2026-09-08 ／ 対象：逆動力学まわり全経路
再現：`python tools/verify_dynamics_recheck.py`
前回：`力学計算_検証結果.md`（2026-09-02）、`KNOWN_ISSUES.md`

---

## この再検算で分かったこと

前回の検証は**オフライン経路**（`compute_torque_from_pose.py`）を中心に見ており、
**リアルタイム経路**（`master_research_code.py` → `LinkVectorCalculator`）は範囲外だった。
今回そちらを解析解に通したところ、**リアルタイム経路は力学量として成立していない**。
最大の原因はキーポイント抽出のループが元実装と違う順序で走っていることで、
`part_calculations` 側は正しい（R-1）。

一方、逆動力学の定式そのものは前回の結論どおり正しい。誤りはすべて「式に何を入れているか」の側にある。

**論文の数値はオフライン経路で出ているため、以下の R-1〜R-4 は論文の数値には効かない。**
効くのは前回から未修正の §1-1・§1-2・§1-3 と、今回見つけた R-5。

---

## 検算の方法

始点を原点に固定し、終点が z 軸まわりに角速度 W で等速回転するリンクを使う。
この系は全量に閉じた解析解を持つ：

```
r(t) = L(cos Wt, sin Wt, 0)     ω = (0,0,W)     ω̇ = 0
r̈ = −W²r  （大きさ W²L）        重心（中点）の加速度 = r̈/2
```

W = 2.0 rad/s、L = 0.25 m、dt = 1/30 s。実データ照合には
`output_data/stereo_2_20250925_162436.npy`（1217 フレーム、MediaPipe 33 点）を使った。

---

## A. 正しいことを確認した（変更不要）

| 対象 | 検証 | 結果 |
|---|---|---|
| 逆動力学の連鎖式 `τ_j = ΣM + Σ(r_g−p_j)×F − τ_E − (r_x−p_j)×f_E` | 静止水平リンクの解析解 `m·g·L/2` | 1.177200 vs 1.177200、**比 1.000000** |
| `F = m(a − g)` と符号規約 | \|F\| = m·g | 9.4176 N で完全一致 |
| オフライン経路の ω, ω̇, 重心加速度 | 等速回転の解析解 | 1.9985 / 0 / 0.4998（真値 2.0 / 0 / 0.5） |
| 角速度の向き | 3D 回転軸との cos | **1.000000**（大きさだけが違う） |
| 純並進での ω | 真値 0 | 0.000（誤差は回転成分にのみ出る） |
| 慣性回帰式（体重を渡した場合） | 一様棒 `mL²/12` | 前腕 0.00463 vs 0.005 |

---

## B. 新たに確定した誤り

### ★R-1【最重大】キーポイント抽出のループが順序を壊している

**原因が特定できた。** 元実装 [TemugeB/bodypose3d](https://github.com/TemugeB/bodypose3D) は

```python
pose_keypoints = [16, 14, 12, 11, 13, 15, 24, 23, 25, 26, 27, 28]
for i, landmark in enumerate(results0.pose_landmarks.landmark):
    if i not in pose_keypoints: continue
```

と **ランドマーク ID の昇順に走査**しており、`pose_keypoints` の並びは
単なるフィルタとしてしか働かない。したがって実際の並びは

```
sorted(pose_keypoints) = [11, 12, 13, 14, 15, 16, 23, 24, 25, 26, 27, 28]
→ [0]=左肩 [1]=右肩 [2]=左肘 [3]=右肘 [4]=左手首 [5]=右手首
  [6]=左腰 [7]=右腰 [8]=左膝 [9]=右膝 [10]=左足首 [11]=右足首
```

これは元リポジトリの図（0,1=肩／2,3=肘／4,5=手首／6,7=腰／8,9=膝／10,11=足首）と一致し、
`master_research_code.py:1653` の `part_calculations` の前提とも**完全に一致する**。
つまり **`part_calculations` は正しい。**

壊しているのは抽出側である。`utils.py:142`

```python
for pid in pose_keypoints:      # ← リストの並び順でループしている
```

`master_research_code.py:2194` の高速版 `_extract_keypoints_fast_single` も同じ。
このため実際の並びは `[16, 14, 12, 11, 13, 15, 24, 23, ...]` の順、すなわち

```
[0]=右手首 [1]=右肘 [2]=右肩 [3]=左肩 [4]=左肘 [5]=左手首 [6]=右腰 [7]=左腰
```

となり、`part_calculations` が意図した並びと**先頭 8 個が食い違う**（後半 `[25, 26, 27, 28]` は一致）。
`config.py:23` のコメント「bodypose3d の順序に合わせて」は、
リストの並びを順序だと解釈した誤読。

実データで測ったリンク長：

| リンク名 | 現状で結ぶ点 | 長さ | 正しい長さ |
|---|---|---:|---:|
| `upper_arm_R` | 左肩 → 右肘 | 0.404 m | 0.237 m（右上腕） |
| `forearm_R` | 左手首 → 左肩 | 0.429 m | 0.205 m（右前腕） |
| `both_shoulder` | 右手首 → 右肘 | 0.205 m | 0.295 m（両肩幅） |
| `both_hip` | 右腰 → 左腰 | 0.195 m | 0.195 m ✔ **唯一正しい** |
| `up_arm_l` | 右肩 → 右手首 | 0.437 m | 0.234 m（左上腕） |
| `forearm_L` | 左肘 → 右肩 | 0.417 m | 0.213 m（左前腕） |
| `upper_Leg_R` | 左腰 → 右膝 | 0.356 m | 0.273 m（右大腿） |
| `upper_Leg_L` | 右腰 → 左膝 | 0.347 m | 0.273 m（左大腿） |

慣性テンソルへの波及（体重 60 kg）:

| 部位 | リンク長 | diag の比（現状 / 正しい） |
|---|---|---|
| 上腕 | 0.437 → 0.237 m | ×7.22, ×7.79, ×1.21 |
| 前腕 | 0.417 → 0.205 m | ×6.61, ×7.11, ×1.88 |
| 大腿 | 0.356 → 0.273 m | ×4.54, ×5.19, ×1.21 |

`len_half_body`（`:3423`）は `0.25·|p0+p1−p7−p6|` だが p0・p1 が手首と肘なので
0.129 m（正しくは 0.216 m）になり、上胴体の慣性テンソルが**負**になる。

#### 同一ファイル内にもう一つ別の規約がある

`master_research_code.py:3630-3637` の `links` と `:1113-1123` の `origin` は

```python
"elbow_R": transformed_p3ds[2] - transformed_p3ds[0],   # 0=右肩, 2=右肘 を前提
"wrist_R": transformed_p3ds[4] - transformed_p3ds[2],   # 4=右手首
```

と、`[0]=右肩 [1]=左肩 [2]=右肘 [3]=左肘 [4]=右手首 [5]=左手首` を前提にしている
（`compute_torque_from_pose.py:54-59` の `LEGACY_*_IDX` と同じ規約）。
これは bodypose3d 準拠の並びに対して**左右のラベルが入れ替わっている**。
リンク構造は正しいので、抽出順を直しても `elbow_R` として出るのは左肘のトルクになる。

#### 影響範囲

位置索引で `transformed_p3ds` を引く経路のみ
── `master_research_code.py` / `master_research_code_00.py` / `twin_video_capture.py` /
`app/runners/network_measure.py`。

**論文の数値には効かない。** 論文に使われた pose CSV（`Adjusted 3D Pose/*.csv`）の列名は
`joint_16_x, joint_14_x, …` と **MediaPipe の ID がそのまま入っており**、
`compute_torque_from_pose.py:178-190` は `jid` を位置索引として展開する。
`:607` の `use_legacy = pose_interp.shape[1] <= 16` は shape[1]=17 で False になるため、
`RIGHT_SEGMENTS`（12/14/16 を直接指定）が選ばれる。列名で引いているので順序の影響を受けない。

#### 修正の選択肢

| | 変更 | 効果 | 残る問題 |
|---|---|---|---|
| **A** | `utils.py:142` と `master_research_code.py:2194` を昇順ループにする | 元実装に忠実。`pose_keypoints` の並びに依存しなくなる | `links`/`origin` の左右ラベル |
| **B** | `config.py:24` の `pose_keypoints` を昇順に並べ替える | 1 行で済む | 並び順依存の脆さが残る |

どちらも `part_calculations`・慣性テンソルのリンク長・`:3532` の地面反力判定・
`:3303`/`:3879` のサイクル検出が一斉に正しくなる。
`links`/`origin` の左右ラベル（計 12 行）は別途あわせる必要がある。

### ★R-2 リアルタイム経路の角速度が別の量

`link_vector_calculator_module.py:95`

```python
cross_val = np.cross(self.previous_velocity_vector, self.velocity_vector)
self.angular_velocity = cross_val / (denom ** 2)
```

標準形は `ω = (r × ṙ)/|r|²` だが、実装は外積の第1引数が **r ではなく前フレームの速度**。
剛体回転 `ṙ = ω×r` を代入して展開すると

```
ṙ_prev × ṙ = dt·|ω⊥|²·ω⊥·|r|²   →   ω_impl = dt·|ω⊥|²·ω⊥
```

次元は **1/s²** であって 1/s ではない。数値でも確認：真値 2.0 rad/s に対し実装 0.2664、
`ω³·dt = 0.2667` と一致（相関 0.975、傾き 0.96、向きの cos = 1.000000）。

**信号は `ω²·dt` 倍に潰れる**：

| 真の ω [rad/s] | 実装が返す値 | 比 |
|---:|---:|---:|
| 1.0 | 0.0333 | 3.3% |
| 1.6 | 0.1365 | 8.5% |
| 3.0 | 0.9000 | 30.0% |

**同時にノイズは増幅する**。`v = Δr/dt` なので位置ノイズが 1/dt = 30 倍され、
その外積が `(√2·σ/dt)²/|r|²` のオーダーの偽信号になる：

| 位置ノイズ | 実装 中央 | 実装 最大 | 正しい式 中央 | 正しい式 最大 |
|---:|---:|---:|---:|---:|
| 0 mm | 0.266 | 0.27 | 1.999 | 2.00 |
| 1 mm | 1.097 | 3.08 | 1.996 | 2.30 |
| 2 mm | 2.081 | 6.13 | 2.001 | 2.61 |
| 5 mm | 5.579 | 19.66 | 2.088 | 3.56 |

**2 mm のノイズで真値と同じ大きさの値が立つ。** 実データ（右前腕）でも
実装は中央 1.44 rad/s・最大 116 rad/s に対し、正しい式は中央 0.69・最大 6.5。
つまり出力は角速度ではなく**ノイズが作った値**。

`ω̇ = Δω/dt` なので角加速度は 1/dt³ でスケールし、`M = I·ω̇` の次元は
kg·m²/s³ = W（仕事率）になる。`ω×(I·ω)` は kg·m²/s⁴ で、第1項とも次元が揃わない。

> `tests/test_dynamics_dt.py::TestScalingLaws` が `ω³·dt` として現状を固定済み。
> 本項はそこに**ノイズ応答と実データでの挙動**を足すもの。

### ★R-3 `dot_dot_pg` に重心加速度ではなく `r̈` を渡している

`link_vector_calculator_module.py:105` の `acc` は
`(velocity_vector − previous_velocity_vector)/dt`、つまり**リンクベクトル `r` の 2 階微分**。
これが `storage.add_data(..., acc)` 経由で `dot_dot_pg`（重心加速度）として
`F = m(a − g)` に入る。

- 始点が固定なら `r̈ = 2 ×（中点の加速度）` → **2 倍過大**（実測 0.99963 vs 0.5）
- 始点が動けば別のベクトル（始点も動かした合成データで相対誤差 1.000）

`centroid`（中点）は計算しているが、その 2 階微分は取っていない。
オフライン側は `com_pos = prox + com_fraction·link` を作ってから微分しており正しい。

### ★R-4 重心位置が中点固定（COM 比 0.5）

`link_vector_calculator_module.py:71` の `centroid` は常に両端の中点。

| 部位 | 実装 | 文献値 | 重力モーメント腕 |
|---|---|---|---|
| 上腕 | 0.500 | 0.436 | **+14.7%** |
| 前腕 | 0.500 | 0.430 | **+16.3%** |

オフライン側は `SegmentSpec` が `com_fraction` を持っていて正しい。

### ★R-5 理論仕事量の係数 K が 2 系統で 9.3% 食い違う ← 論文の数値に効く

| 実装 | 係数 | 値 |
|---|---|---|
| `master_research_code.py:972`、`offline_wrist_energy.py:35` | `√3/2 + 1` | **1.8660** |
| `compute_cycle_energy_elbow_wrist.py:141`、`compute_elbow_cycle_work.py:60`、`diag_elbow_checks.py:128` | `16.73` = `(√2/2 + 1)·9.8` | **1.7071**（×g） |

同じ「1 サイクルの角度範囲にわたる `cos θ` の積分」を表す係数のはずが、

```
∫_{−π/2}^{π/4} cos θ dθ = √2/2 + 1 = 1.7071   （−90°〜45°）
∫_{−π/2}^{π/3} cos θ dθ = √3/2 + 1 = 1.8660   （−90°〜60°）
```

と**積分範囲が違う**。前回記録の C 節は 16.73 側だけを「導出も値も正しい」としており、
`√3/2 + 1` 側は未カタログ。**どちらの角度範囲が論文の定義かの確認が要る。**

あわせて重力加速度も 3 つ併存：`config.g` = 9.81、`offline_wrist_energy.G` = 9.80665、
`16.73` に内包された 9.8。

### ★R-6 慣性テンソルのリンク長が 3 フレーム目の瞬時値で固定される

`master_research_code.py:3416-3437` は `len(kpts_3d) == 3` のときだけ I1〜I7 を計算して
`continue` する。`kpts_3d` は毎フレーム append される無制限リスト（`:1896`, `:3294`）なので、
**この条件が成立するのは実行中ただ 1 回**。その瞬間の三角測量誤差がそのまま全実行に固定される。

オフライン側は `np.nanmedian(lengths[:, idx])`（`compute_torque_from_pose.py:324`）で頑健。
慣性回帰式は L に極端に敏感（`KNOWN_ISSUES` §1-4）なので、瞬時値固定はリスクが大きい。

### ★R-7 回帰式が短いリンクでは体重を渡しても負を返す

下腿（行 7）に体重 60 kg・L = 0.232 m を入れると `diag = [−0.0156, −0.0156, +0.0247]`。
定数項 `c = −1190.24` が `b·l` と相殺する領域に入るため。
`KNOWN_ISSUES` §1-3 は「体重を渡せば正常」としているが、**体格によっては体重を渡しても負になる**。
回帰式（`Moment of inertia estimation coefficient boys.csv`）の適用範囲の確認が要る。

---

## C. 前回から未修正のまま（再確認）

| 項目 | 場所 | 状態 |
|---|---|---|
| §1-1 ω に fps を余計に掛けている | `compute_cycle_energy_elbow_wrist.py:231-232` | **未修正** |
| §1-2 関節角を unwrap していない | 同上 `_angle_about_y` | **未修正** |
| §1-3 慣性回帰式に部位質量を渡す | `compute_torque_from_pose.py:326` | **未修正** |
| §2-3 慣性テンソルを回転させない | `utils_dynamic.py:110` の `M = I·ω̇ + ω×(I·ω)` | 未修正（限界として明記の方針） |

---

## D. 対応の優先度

**論文の数値に効く順**：

1. **§1-1・§1-2** ── 各 1 行。スコアが約 1/350 になる（前回記録どおり）
2. **R-5** ── 積分範囲 −90°〜45° か −90°〜60° かを決める。9.3% の差
3. **§2-1** ── 体幹荷重を入れるかどうか。論文の主張そのもの
4. **§1-3・R-7** ── 慣性回帰式の引数と適用範囲

**リアルタイム表示の妥当性に効く順**（論文の数値には効かない）：

5. **R-1** ── 抽出ループを昇順にする（案 A）か `pose_keypoints` を並べ替える（案 B）。
   `part_calculations` は bodypose3d 準拠で正しいので触らない。
   あわせて `links`/`origin` の左右ラベルを揃える
6. **R-2** ── `ω = (r × ṙ)/|r|²` に直す。`tests/test_dynamics_dt.py` の 2 テストと
   docstring もあわせて更新すること
7. **R-3・R-4** ── 重心位置に `com_fraction` を入れ、その 2 階微分を `dot_dot_pg` にする
8. **R-6** ── リンク長を中央値で決める
