### Core numerical / vision
numpy
scipy
pandas
# mediapipe が opencv-contrib-python を要求する。opencv-python と両方入れると
# 同じ cv2 ディレクトリに上書きで入り、どちらが使われるか分からなくなる。
opencv-contrib-python
matplotlib

# mediapipe のバージョンは固定する。
# master_research_code.py:1299-1301 が旧 Solutions API（mp.solutions.pose 等）を
# 参照しているが、これは mediapipe 1.0 と 0.10.20 以降で削除されている。
# 0.10.14 は Solutions API を持ち、Python 3.12 の macOS arm64 wheel もある。
# （0.10.9 以前は cp312 の wheel が無い）
mediapipe==0.10.14

pygame
pillow
pyyaml

### GUI (Gauge_display.py が pyqtgraph.Qt 経由で使う)
# pyqtgraph は Qt バックエンドを自動選択する。PySide6 は LGPL なので配布時に
# ソース開示義務が生じない。PyQt5 は GPL v3 のため、配布物には入れないこと。
# バックエンドを確定させたい場合は PYQTGRAPH_QT_LIB=PySide6 を設定する。
pyqtgraph
# 計測画面が QFormLayout.setRowVisible（Qt 6.4 から）を使う
PySide6>=6.4

### スマホ接続用の QR 表示（PC が接続先を端末に渡す手段）
segno

### Networking / I/O
# スマホからランドマークを受け取る WebSocket サーバ（app/net/server.py）。
# websockets.asyncio.server は 13 以降にある。
websockets>=13
requests
boto3
pyserial
bleak

### Platform-specific
wmi; platform_system == "Windows"
comtypes; platform_system == "Windows"
