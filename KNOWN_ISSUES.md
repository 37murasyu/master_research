# 要修正一覧

最終更新：2026-09-23 ／ 対象：このリポジトリ（旧 `MAINCODE/`）
検証の詳細は `力学計算_検証結果.md`（2026-09-02）と `力学計算_再検算_2026-09-08.md`。
EKF の自己チューニングは `docs/superpowers/specs/2026-09-08-ekf-self-tuning-design.md`。
修士論文本文側の修正は `修論_修正箇所リスト.md`（このリポジトリの外）。

§5-1〜§5-6 と §6 は、2026-09-09 の計画メモ「力学モデルの確定と、スコア再計算までの障害除去」（リポジトリ外）から
持ち越した項目（§1-5・§1-6・§5-7・§5-8 は 2026-09-13 の左右差の切り分けで見つかったもの）。
コミットメッセージが参照する番号（A-4、E-1、H-B など）を括弧で併記している。
§1-7・§2-6・§6-4 は 2026-09-23 の一括対応で見つかったもの。対応の計画と経過は
`docs/superpowers/plans/2026-09-23-remaining-tasks.md`。§3-3・§3-4・§6-8・§6-9 は同日、USB の録画と再生で
§6-2・§6-3・§3-2 を確かめる道具を作ったときに見つかったもの（手順と試走の結果は `docs/usb_stereo_verification.md`）。

優先度は **論文の数値に効くか** で並べている。§1 は結論そのものが動く。

---

## 残タスク（2026-09-23 時点）

2026-09-23 の先行対応で、判断待ちだった項目を次のとおり決めた。追加の引き継ぎ対応は下記と計画末尾に記録する。

| 項目 | 決定 |
|---|---|
| §1-5 重力 | 初期フレームの体幹の向きから推定する（最寄りの座標軸へ吸着。カメラが水平という前提） |
| §2-2 等価質量 | 分母に手を含める（現行どおり）。分子の鎖は手を固定端とするので手を含めない |
| §5-7 関節ラベル | `wrist_R` を右手首まわりのトルクにする（3 経路とも `push_up_model` に一本化） |
| §5-4 局所座標系を作れないフレーム | 警告する（値は今のまま全体座標） |
| H-A 体幹の質量比 | 今の比（Winter、60 kg で 34.68 kg）で確定 |
| §4-1 ラベル描画 | スプライトに一本化（ネイティブ描画は到達不能のまま使われていなかったので分岐を外した） |
| §2-6 手首の分母 | 定義を直す: ダンベルは手のひらにあり、てこの腕は手の中心（手長 × 0.506。手長 = 前腕長 × 0.108/0.146）。論文の定義（rx = 0）を絶対視しない |
| §6-4 1RM の列 | `elbow_*_outer` は「肘が内側へ曲がろうとするトルクを打ち消す向き」の筋力（肘の伸展）。プッシュアップの肘も伸展なので、これを使う |
| §6-1 前処理 | カットオフを試技ごとに f0 から決める（fc = 6·f0 を 2.1〜6.0 Hz に収める。リアルタイム経路・論文の表 5 と同じ）。`pose_lowpass.py` |
| §6-8 肘のゲージ | 手首と同じく、サイクルごとにリセットする（起動からの累積だった） |
| §3-3 出力先 | 計測の CSV を GUI の表示どおり `~/Documents/WheelchairTorque/output_data` に書く |

### 判断待ち・保留

| 項目 | 決めること | 効く先 |
|---|---|---|
| **§6-9（新規）** | USB 経路のサイクル検出が左肩の **y**（奥行き）を 1 フレームあたりの速さで見ており、間引きなし（GUI の既定）では 1 回も検出しない。上の軸（重力の推定結果）と秒あたりの速さに直すか | ゲージのリセット・サイクルの記録・`RT_DYN_ON_RISE_ONLY` の力学の開始 |
| §6-1 | 集計に入れるファイル（3_1・4_0・5_stereo はサイクル範囲の表に無い。5_1 は左前腕 0.68 m などデータの質が怪しい）と、論文どおり右側だけを報告するか | スコア |
| §1-5 | 重力の決め方の既定（`axis` と `trunk` で手首・肘のトルクが最大 2 倍違う。カメラが水平だったか） | オフラインのトルク |
| §1-4・§6-6 | 本文の記述と計算の食い違い（I_xx、慣性項を「無視した」、GCVSPL、図の分母など）をどう直すか | 本文 |
| §2-6 | 手首の 1RM に `wrist_*` と `wrist_*_inner` のどちらを使うか（今は `wrist_*`） | 手首のスコアの分母 |
| §6-4 | `elbow_*_outer` が元のスプレッドシートのどの列か（値は受領した `m_max_all_merged.csv` から回収済み） | 肘のスコアの分母の出典 |
| §6-8 | ゲージの閾値の式が旧来のまま（手首の r_x = 0.30 m 固定、帯は約 95〜128 J）。1 サイクルの正の仕事は 1〜12 J 程度で帯に届かない | ゲージの表示 |

### 実機で行う

手順は、USB カメラ 2 台なら `docs/usb_stereo_verification.md`、実際の構成の Mac＋Pixel 7a（混成ステレオ）なら
`docs/hybrid_verification.md`（道具）と `docs/hybrid_field_run.md`（当日の手順・記録シート。先に置き方を直す。§6-10）。どちらも `tools/verify_run.py check` で確かめる（混成の計測フォルダは自動で見分ける）。
USB は受け取った被験者 7 の動画の再生で、混成は模擬 Pixel で試走した（下の各節）。実機でしか確かめられないのは次の 3 つ。

1. §6-2 ライブ計測での 30 fps（再生は処理が遅くても dt が正しいので、速さの上限しか分からない）とゲージの値
2. §3-2 GUI の停止ボタンで CSV が書かれること（停止ファイルの経路は再生で確認済み）
3. §6-3 新しい構成の録画での S6 → S9b。同梱既定値と `EKF_MAX_GAP_S` の既定値を実測で決める。
   試走では設計メモの中止条件（端に張り付く系列が過半数・|ρ1| > 0.3）に当たった

### Windowsデータの受領・照合（2026-09-23）

`~/Downloads/transfer_20260923.zip` を受領し、同所の `transfer_20260923_review/extracted/` に原本として展開。
一覧361件のパス・サイズと全ZIPエントリーのCRCは一致した。提示SHA-256とは異なるが、送信側の最終SHAは確認できないとのユーザー回答を受け、受領版を解析の基準とする。送信側との同一性は未確認として記録し、再作成・再送や追加照合を待ち条件にしない。
詳細・出力先は [Windows転送データの照合](docs/windows-transfer-audit-2026-09-23.md)。

- 手の点付き処理済み姿勢は無し。3・4・5・6・7・8の動画18本はあるが、校正・単位を検証して再推定する作業が残る。
  被験者 7 の動画は、USB 経路の再生（`tools/verify_run.py replay`）で骨長が妥当に出ることを確かめた（§6-2）。
- `m_max_all_merged.csv` のouter列を回収（リポジトリ直下に写した。`*.csv` は追跡外）。元シート列との対応は未確定（§6-4）。
- 論文0.99をCSVから再現し、分母14件は旧ゲージ式と一致（§6-6）。旧torque_wristbaseの0.01倍率も8試技で再現（§1-7）。
- §3-1の7件隔離はWindows側の報告あり。ただし移動後の証跡はZIPに無く、独立確認は未完了。

### いつでもよい

§2-3 の残り（軸まわりの回転は取れない、限界として本文に書く）・上腕質量比 0.0227 の出典確定・
§6-5 の集計対象と単位の注意・スマホ経路の実機校正とGUI／保存への接続。

**引き継ぎ追加対応（2026-09-23、コミット d3ce0ee・13a34b1・9a72f2c・cc61163）**:

- §4-4: 残り7か所と `E_FC_ADAPTIVE_ON` を `env_flag` に統一し、設定スキーマを再生成。
- §4-1: ネイティブ描画の実装・ビルド生成物・専用の旧手動テストを削除。
- §6-5: 範囲外サイクル検出と旧トルク推定CLIの出力APIを修正。
- 等価質量係数のキーを `upper_limb` から `torso` に改名。値と分母は変更しない。
- ノイズ評価の被験者4の固定除外を解除。除外は `--exclude-subjects 4` で明示する。
  被験者4のサイクル範囲は未確定であり、8名の再集計は未実施。
- PCカメラのランドマーク送信器を追加。スマホ1台との同期を模擬入力で確認。
  起動手順と残る統合は `docs/phone_pc_capture.md`。
- ユーザー確認: Windowsデータは受領済み（上記）。撮影時の水平設置は不明のため、重力の既定は現状維持。

**定義の修正と検証の道具（2026-09-23 の続き）**:

- §2-6・§6-4: 手首の分母の定義を直し、肘の 1RM に伸展の筋力（outer）を使う（7e589b2）。
- §6-1: 前処理のカットオフを試技ごとに f0 から決める `pose_lowpass.py`（aa9197f）。
- §6-8: 肘のゲージをサイクルごとにリセットし、ゲージの値と閾値を HEADLESS でも `gauge_energy_*` に残す（`gauge_energy.py`）。
- §3-3: 計測の CSV を GUI の表示どおりの場所に書く（`OUTPUT_DIR`、`app.core.settings.measurement_output_dir`）。
- USB の録画 `tools/record_stereo.py`、再生と検証 `tools/verify_run.py`、カメラ設定の共有 `app/core/camera_controls.py`。
  手順は `docs/usb_stereo_verification.md`。
- `murayama/hybrid-stereo`（Mac＋同じ Wi-Fi の Pixel 7a）を統合した（a29ee51）。混成の計測も `tools/verify_run.py check` で
  確かめられるようにし（§3-2 は `meta.json` の状態と止まった理由、§6-2 はトルク・サイクルの仕事・Mac と Pixel の fps）、
  S6 用に 3D を生 CSV へ直す `hybrid-raw` を足した。混成の計測は止まった理由（`stop_reason`）を `meta.json` に残す。
  手順は `docs/hybrid_verification.md`。
- 実機の Pixel 7a は 10〜15 Hz（hybrid ブランチでの実測、2026-09-23 にユーザーが報告）。30 Hz の組の Pixel 側は大半が線形補間の点に
  なるので、`check` は Mac・Pixel のどちらかが 24 fps を下回ると「速さ」を不合格にし、実測に基づく組の割合を出す。
  `hybrid-raw` は記録した 2D から Pixel の実際の撮影時刻で三角測量し直す（`app.hybrid.retriangulate`、`--hz 4` で 4 Hz 相当）

---

## 対応状況

| 項目 | 状態 | コミット |
|---|---|---|
| §1-1 角速度に fps を掛けている | **修正済み** | a44950d |
| §1-2 関節角を unwrap していない | **修正済み** | a44950d |
| §1-3 慣性回帰式に部位質量を渡している | **修正済み**（短いリンクで負になるガードも追加） | a44950d, 5336215 |
| §1-4 本文の I_xx と実測前腕長の食い違い | 未対応（本文側の判断が要る） | — |
| §1-5 オフライン経路の重力が奥行き方向を向いている | **修正済み**（初期フレームの体幹から推定） | 2026-09-23 |
| §1-6 `compute_cycle_noise_contrib.py` に §1-1 が残っている | **修正済み**（スコアと同じ射影を使う） | 2026-09-23 |
| §1-7 オフラインのトルク倍率の既定が 0.01 | **修正済み**（既定 1） | 2026-09-23 |
| §2-1 体幹荷重がモデルに入っていない | **修正済み**。3 経路とも同じモデル、`--wrist-base` は既定オン | 6a9ffa2, 2026-09-23 |
| §2-2 分子と分母で等価質量の定義が違う | **決定**（分母に手を含める。差は動作の違い） | 2026-09-23 |
| §2-3 慣性テンソルを回転させていない | **修正済み**（軸対称近似で回す）。軸まわりの回転は限界として残る | 2026-09-23 |
| §2-4 2 点リンクは軸まわり回転を取れない | 限界として明記済み（テストにも記載） | — |
| §2-5 加速度がノイズを増幅する | 限界として明記済み | — |
| §2-6 手首の分母でダンベルのてこの腕が 0 | **修正済み**（てこの腕は手の中心。1RM の列 wrist / wrist_inner は保留） | 7e589b2 |
| §3-1 壊れた別系統の出力 | Windows側で7件隔離の報告。移動後証跡は未受領 | — |
| §3-2 GUI から停止すると終了時の CSV が書かれない | **修正済み**（停止ファイル。再生で停止ファイルの経路を確認。GUI の停止ボタンは要実機確認） | 130d8bf |
| §3-3 GUI の「出力先」と実際の出力先が違う | **修正済み**（`OUTPUT_DIR`） | 未コミット |
| §3-4 計測自身の録画は時間が詰まる | 記録のみ（録画は `tools/record_stereo.py` で撮る） | — |
| §4-1 ネイティブ描画パスが既定で到達不能 | **解消**（スプライトに一本化） | 2026-09-23 |
| §4-2 ラベル描画の二重実装 | **解消**（`utils.draw_text_jp`） | 2026-09-23 |
| §4-3 `LandmarkEKF` の 2 実装と `h_fn` の無視 | **解消** | c792b41, feef20a, b3b76e1 |
| §4-4 その他（小） | **解消**（計時変数、真偽値設定の統一） | 2026-09-23、13a34b1 |
| §5-1 手首の基準軸が肘の屈曲軸のまま | **修正済み**（手のひら。手の点が無ければ腕の面の法線） | 2026-09-23 |
| §5-2 仕事率の ω が τ の軸と揃っていない | **修正済み** | 22459ce |
| §5-3 左右差 | **切り分け済み**。残りはデータ由来 | 5ad3493, 22459ce |
| §5-4 局所座標系を作れないとき | **決定・修正済み**（警告） | 2026-09-23 |
| §5-5 定義が使われていない／出典が不明 | **確認済み**（大腿は Winter。上腕 0.0227 は出典不明のまま） | 2026-09-23 |
| §5-6 出力ファイルに規約の版が無い | **修正済み**（`OUTPUT_SCHEMA_VERSION = 2`） | 2026-09-23 |
| §5-7 リアルタイム経路の関節ラベルと基準点 | **修正済み** | 2026-09-23 |
| §5-8 リアルタイム経路に体幹荷重のモデルが無い | **修正済み** | 2026-09-23 |
| §6-1 スコアの再計算 | **実施**（§2-6 の定義修正・outer・f0 のカットオフで再計算。集計範囲は判断待ち） | 7e589b2, aa9197f |
| §6-2 実機確認 | 道具と手順を用意し、再生で試走（被験者 7）。ライブの 30 fps・GUI の停止は要実機 | 未コミット |
| §6-3 EKF の自己チューニング | S1〜S5・S7〜S11 済み。S6 は再生で試走し中止条件に当たった（新しい構成で取り直す）。S9b が残り | d4b3a79〜93f1723, 2026-09-23 |
| §6-4 1RM の `elbow_*_outer` | **決定**（伸展の筋力として使う）。元シートの列は未確定 | 7e589b2 |
| §6-8 ゲージの値 | **修正済み**（肘をサイクルごとにリセット、HEADLESS でも記録）。閾値は USB が保留、混成は論文の W_0.70・W_0.85（`app/gauge/thresholds.py`）。被験者ゲージの窓は混成だけ | 2026-09-24（報告書 `docs/superpowers/reports/2026-09-24-hybrid-port-report.md`） |
| §6-9 サイクル検出 | USB は**判断待ち**（間引きなしで検出しない）。混成は高さ・m/s の `RepDetector` で作り直した | 2026-09-24 |
| §6-10 実機の混成計測で 3D が壊れる（置き方） | 道具・手順・アプリを用意。混成に EKF・体格の検査（終了コード 3）・力学の関所・骨の長さの見張りを入れた。置き直しは実機で | 2026-09-24 |
| 再検算 R-1〜R-7 | **すべて修正済み** | 4077b28, dc28a30, b7e308a, 5336215 |
| 計画 A-1（リンク定義の ID 化）・A-3（手の点の追加） | **修正済み** | e9fdca7, 8e20586 |
| 計画 B-1〜B-3（パイプラインの障害） | **修正済み** | 4b2a68e, 560ebd9 |

再検算の検算は `python tools/verify_dynamics_recheck.py`。自動判定は `tests/`（`pytest tests`）。
2026-09-23 の変更の主なテストは `tests/test_push_up_model.py`（モデル）、`tests/test_path_consistency.py`
（3 経路が同じ姿勢から同じトルクを出す）、`tests/test_offline_torque.py`（座標系に依らない）。

---

## §1 力学計算の誤り（最優先・数値で再現済み）

### 1-1 角速度に fps を余計に掛けている → スコアが正確に30倍 【修正済み 2026-09-08】

`compute_cycle_energy_elbow_wrist.py:229-230`

```python
elbow_omega = _gradient(elbow_angle, dt) * (args.fps if args.fps > 0 else DEFAULT_FPS)
```

`_gradient`（`:48-51`）は `np.gradient(series, dt)`。第2引数 dt はサンプル間隔なので
戻り値は既に rad/s。そこへ fps=30 を掛けている。

- 実測 ω：平均 **47.5 rad/s**、最大 **2827 rad/s**（450回転/秒）
- 修正後：平均 1.59 rad/s

**兄弟実装 `compute_elbow_cycle_work.py:100` は `gradient(ang, dt)` のみで正しい。**
同一プロジェクト内で2実装が30倍食い違っている。

**修正**：`* (args.fps ...)` を削除。

### 1-2 関節角を unwrap していない → ±π の折り返しが偽の巨大 ω を生む 【修正済み 2026-09-08】

`_angle_about_y`（`compute_cycle_energy_elbow_wrist.py:33-38`）は `arctan2` なので値域 ±π。
実データに **1試技あたり中央29回・最大58回**の折り返しがある。1回につき ω に 2π/dt = 188 rad/s の
スパイクが立ち、`W_pos = Σ max(τω,0)·Δt` は正側だけ拾うので必ず加算される（整流）。

**`compute_wrist_cycle_work_from_npy.py:87` は `np.unwrap(theta)` 済み。この1本だけ抜けている。**

**修正**：`np.gradient(np.unwrap(ang), dt)`。

### 1-1 + 1-2 を直したときのスコア

被験者3,5,6,7,8（m単位データ、両側）の `ratio_pos_vs_1rm`：

| | 平均 | 中央 |
|---|---|---|
| 修正前の実装 | **1.076** | 0.810 |
| 1-1 だけ修正 | 0.036 | 0.027 |
| 1-1 + 1-2 修正 | **0.004** | 0.003 |

> **2026-09-23 受領データによる訂正**: 上表はLPF系・左右合算の検証値で、本文の0.99の再現ではない。
> 本文の0.99は別のGCVSPL系CSVの右肘・右手首7名の中央値の平均0.98631で再現できた。
> その分母14件は旧ゲージ式と一致し、試技・サイクルも異なる（§6-6）。
> したがって上表から「本文の中心的数値が約1/350になる」とは結論できない。
ただしこれは §2-1 の体幹荷重を入れる前の値。

### 1-3 慣性モーメント回帰式に部位質量を渡している（正しくは体重）→ 慣性テンソルが負 【修正済み 2026-09-08】

`compute_torque_from_pose.py:326-328`

```python
mass = body_mass * seg.mass_fraction
inertia_tensors[idx] = calculate_inertia_tensor(seg.inertia_row, mass, length)
```

回帰式 `I = a·w + b·l + c`（`utils_dynamic.py:50-52`）の w は**全身体重**。

| | 修正前（部位質量） | 修正（体重60kg） | 教科書値 |
|---|---|---|---|
| 前腕 diag（L=0.191） | **[−0.00376, −0.00355, −0.00082]** | [0.00132, 0.00118, 0.00060] | — |
| 上腕 diag（L=0.299） | **[−0.00135, −0.00105, 0.00033]** | [0.00950, 0.00915, 0.00449] | — |
| 大腿 I_xx（L=0.42） | 0.0330 | **0.0969** | 0.110（Winter, k=0.323L） |

前腕は全軸で負。物理的にあり得ない。
**リアルタイム側 `master_research_code.py:3319-3339` は `w`（体重）を渡していて正しい。**
オフライン再計算側だけの誤り。

**修正**：`calculate_inertia_tensor(seg.inertia_row, body_mass, length)`。
影響は肘トルクで平均2.8%（慣性項の寄与自体が1.7〜4.6%）。

> **補足（2026-09-08）**: 体重を渡しても短いリンクでは負になる。体重 60 kg で
> 対角が正になる最小リンク長は 上腕 0.205 / 前腕 0.168 / 大腿 0.249 / 下腿 0.283 m で、
> 実測の下腿 0.232 m は範囲外。`calculate_inertia_tensor` に警告と一様棒近似への
> フォールバックを入れた（再検算 R-7）。

### 1-4 本文の I_xx = 74.33×10⁻⁴ は前腕長 0.30 m の値

前腕行に w=60, L=0.2999 で 74.33×10⁻⁴ に完全一致。ところが本文は実測前腕長を 0.19〜0.26 m と書いている。

| L [m] | 0.19 | 0.25 | 0.30 |
|---|---|---|---|
| I_xx [×10⁻⁴] | 12.6 | 46.3 | 74.4 |

定数項 c = −145.87 が b·l とほぼ相殺する領域なので L に極端に敏感。**要判断**。

実装はリンク長を先頭 30 フレームの中央値で決めるようになった（再検算 R-6、b7e308a）。
本文の値はこの長さとは別物なので、本文側で揃える。

### 1-5 オフライン経路の重力が奥行き方向を向いている 【修正済み 2026-09-23】

`compute_torque_from_pose.py:609` は重力を `[0, 0, −g]` に置く（外部荷重 `:646`、体幹荷重 `:698` も同じ向き）。
ところが入力の `Adjusted 3D Pose/*.csv` は**カメラ座標系で y が鉛直下向き**、z は奥行き。
途中の `filter_pose3d.py`・`recalc_elbow_local_torque.py` に軸の変換は無く、後段は y を鉛直として扱っている
（サイクル検出は右肩の y、`auto_detect_cycles_pose_ranges.py:23`）。

体幹の上向き（肩中点 − 腰中点）の単位ベクトル:

| ファイル | 上向き |
|---|---|
| 2・3_0・3_1・4_0・5・6・7・8・kpts3d_9 | [0.05〜0.15, **−0.97〜−0.99**, −0.09〜−0.19] |
| `5_1stereo_pose_scaled.csv` | [0.11, −0.31, **0.95**]（これだけ z が上。サイクル検出と集計に登場する） |

被験者 3（`3_0`）で重力の向きだけを変えた |τ| 中央値（N·m、`--wrist-base`）:

| | 手首 R | 肘 R | 肩 R | 手首 L | 肘 L | 肩 L |
|---|---|---|---|---|---|---|
| 現行 −z | 95.2 | 53.3 | 5.7 | 115.2 | 74.8 | 7.6 |
| +y（正しい向き） | 28.0 | 15.8 | 1.9 | 11.4 | 24.9 | 2.6 |

手首トルクは「体幹荷重 17.3 kg × g × 肩と手首の、重力に垂直な面内の距離」でほぼ説明できる。
その距離が現行では 0.52 / 0.63 m（腕の鉛直方向の長さ）、正しい向きでは 0.15 / 0.07 m。
**§2-1 の「肘が生理学的な目安 50〜80 N·m に収まる」は、この誤りの結果だった。**

オフライン経路のコード自体は左右対称（鏡映テストで誤差 0）。53.3 vs 74.8 の左右差は、体がカメラに対して
斜め（左肩 z = 4.03 m、右肩 z = 3.72 m）なことにこの誤りが重なって出ていた。

この行は git 履歴の最初（ca4e98d）からあった。

**対応（2026-09-23）**: 重力を初期フレーム（既定 30 フレーム、試技前の安静座位）の体幹（腰中点 → 肩中点）の
向きから決める（`push_up_model.estimate_gravity`）。既定の `--gravity-mode axis` は体幹の向きに最も近い
座標軸を上とする（USB 経路の `GRAVITY_AUTO_DETECT` と同じ考え方で、カメラが水平という前提）。`trunk` は
体幹の向きそのものを上とする。推定結果は `*_meta.json` の `gravity` に残る。`utils.compute_local_torque` の
フォールバック軸は、重力の逆向きを先頭に試すようにした（`up_axis`）。

全 10 本の推定: 9 本は +y（カメラ座標の鉛直下向き）、5_1 だけ −z。体幹の傾きは 4〜18°。
5_1 は手首・肘のトルクが修正前と一致した（もともと −z が正しかった）。
新しいトルクの |τ| 中央値（N·m、2 Hz の前処理後、既定のモデル）:

| ファイル | 手首 R | 肘 R | 肩 R | 手首 L | 肘 L | 肩 L |
|---|---|---|---|---|---|---|
| 2_stereo_pose | 19.8 | 14.2 | 2.0 | 21.4 | 11.7 | 1.7 |
| 3_0stereo_pose_scaled_with2d | 27.6 | 15.6 | 1.9 | 11.2 | 24.3 | 2.4 |
| 3_1stereo_pose_scaled_with2d | 29.6 | 19.6 | 2.4 | 11.7 | 25.7 | 2.4 |
| 4_0stereo_pose_scaled_with2d | 30.4 | 13.6 | 2.0 | 17.9 | 23.0 | 2.7 |
| 5_1stereo_pose_scaled | 48.5 | 31.0 | 4.0 | 61.5 | 38.2 | 5.2 |
| 5_stereo_pose_scaled_with2d | 33.1 | 16.6 | 2.0 | 16.8 | 34.3 | 3.0 |
| 6_stereo_pose_scaled_with2d | 38.7 | 37.4 | 3.9 | 16.3 | 31.4 | 2.5 |
| 7_stereo_pose_scaled_with2d | 22.5 | 19.7 | 2.1 | 20.1 | 19.9 | 2.0 |
| 8_stereo_pose_scaled_with2d | 29.8 | 23.8 | 2.7 | 6.2 | 26.5 | 1.6 |
| kpts3d_9_20250925_201442 | 23.1 | 19.7 | 2.1 | 12.1 | 25.7 | 1.7 |

肩は腕を肩から吊った自重だけ（体幹荷重は肩に載る外力なのでモデル上は肩のモーメントを作らない）。

**USB 経路の自動検出も壊れていた（2026-09-23 のレビューで判明、修正済み）**: `master_research_code.py` の
`_pick_axis_from_vector` は |cos| で軸を比べており、同じ軸の + と − が必ず同点になって、同点のときの優先ラベル
（`GRAVITY_PREFERRED` の逆）が常に選ばれていた。入力に依らず 'Y+' が上になり、実行時の座標（−X, −Z, −Y）では
90 サンプル後に重力が奥行き方向へ切り替わっていた（それまでは config の −z）。いまは慣性テンソルを確定する先頭
30 フレームの体幹から `estimate_gravity` で決め（力学はそれまで回らない）、使った g を生 CSV のサイドカーに残す。
軸の選び方は符号つきの `push_up_model.nearest_axis` にし、チェッカーボードから決める経路も同じ関数を使う。

**感度（判断待ち）**: `axis` と `trunk` で手首・肘のトルクが最大 2 倍違う（被験者 3 の右手首 27.6 / 21.4、
被験者 7 の右手首 22.5 / 9.6 N·m）。座位の体幹は 10° 前後傾いているので体幹そのものを鉛直とみなすと重力が
同じだけ傾き、水平のてこの腕（0.1 m 級）が大きく変わる。カメラが水平に置かれていたかで決まる。

### 1-6 `compute_cycle_noise_contrib.py` に §1-1 の誤りが残っている 【修正済み 2026-09-23】

`compute_cycle_noise_contrib.py:246-247` が角速度に fps を掛けている（§1-1 と同じく 30 倍）。
角度から求める ω（§5-2 で直した左右の符号ずれ）もそのまま。論文の「ノイズ整流」の議論に使っているなら直す。

**対応**: スコアと同じ τ_y・ω_y（`compute_cycle_energy_elbow_wrist._joint_projections`）を分解するようにした。
角度も fps も経由しない。`tests/test_cycle_noise_contrib.py` が「分解する前の仕事がスコアと一致する」を固定する。

### 1-7 オフラインのトルク倍率の既定が 0.01 → m 単位の入力でトルクが 1/100 【修正済み 2026-09-23、新規】

`compute_torque_from_pose.py` の `--torque-scale` の既定が 0.01（N·cm → N·m 用）だった。入力の
`Adjusted 3D Pose/*.csv` は m 単位なので、付け忘れると全トルクがちょうど 1/100 になる。
`*_meta.json` に倍率が残らないので、後から見分けられなかった。既定を 1 にし、meta に `torque_scale` を残す。

`recalc_torque_wristbase.py`（論文の `torque_wristbase` を作ったスクリプト、Windows のパスが直書き）は
`--wrist-base` だけを付けて呼んでおり、倍率は既定の 0.01 のままだった。§2-1 の「修正前は肘 0.50〜0.86 N·m／
手首 0.82〜1.58 N·m」は、100 倍すると 50〜86／82〜158 N·m で、旧来の荷重（55.36 kg）と §1-5 の重力の誤りを
含む wrist-base の値の桁に合う。**「計算していたのは腕の分節を振る仕事だった」という §2-1 の解釈は、倍率 0.01 の
結果だった可能性がある**。

**受領データで確認**: Windows当時のGit版 `2fb8ff6` の力学関数で、受領 `torque_wristbase` の8試技・全体座標18列を
倍率0.01で再現した（最大絶対差1.25e-10 N·m以下）。m入力の3・5・6・7・8では1/100が確認できる。
cm入力の2・9は位置・慣性の単位も絡む。`filtered_torque_lpf_recalc` とGCVSPL系の分子の生成過程全体は未再現で、
すべての旧スコアを一律100倍して直せるという意味ではない。詳細は [転送照合](docs/windows-transfer-audit-2026-09-23.md)。
cm 単位のファイル（`2_stereo_pose.csv`、`kpts3d_9_*.csv`）は `--pos-scale 0.01` を付ける。

---

## §2 モデルの妥当性（バグではないが主張に直結）

### 2-1 分子の力学モデルに「持ち上げている体重」が入っていない 【モデル確定・実装済み 2026-09-09】

修正前は `output_data/filtered_torque_lpf_recalc/` の実測トルクが **肘 0.50〜0.86 N·m／手首 0.82〜1.58 N·m** で、
計算していたのは**腕の分節を振る仕事**だった。§1 を直すとスコアが 0.004 まで落ちるのは本質的にこれが原因。

**確定したモデル**: 車椅子のアームレストを両手で押す動作を、手を外力との境界（固定端）とし、
そこから近位（前腕 → 上腕 → 肩）を持ち上げる逆順鎖として解く。肩より近位の体幹と頭は
「肩に載る外力」に集約し、脚は床／フットレストが支えるので含めない。分母は 1RM のまま変えない。

`compute_torque_from_pose.py --wrist-base` の持ち上げる質量の既定を「体重 − 両腕」（60 kg で 55.36 kg）から
「体幹＋頭」（34.68 kg）に変えた（6a9ffa2）。被験者 3 の |τ| 中央値（N·m、グローバルノルム）:

| | 手首 | 肘 | 肩 |
|---|---|---|---|
| 右 | 95.2 | 53.3 | 5.7 |
| 左 | 115.2 | 74.8 | 7.6 |

**この値は §1-5 の重力の向きの誤りを含んでおり無効。** 正しい向き（+y）では手首 28.0 / 11.4、
肘 15.8 / 24.9、肩 1.9 / 2.6 N·m（右 / 左）。

**残り**:

- ~~`--wrist-base` が既定オフ~~ → 既定オン（`--no-wrist-base` で旧来の鎖だけ）。2026-09-23
- 体幹の質量比は Winter で確定（H-A、2026-09-23）。`config.SEGMENT_MASS_FRACTIONS`。`--torso-mass` で上書きできる
- スコアは再計算した（§6-1）。USB・スマホ経路も同じモデルになった（§5-8）

### 2-2 分子と分母で等価質量の定義が違う

| | 等価質量 m_x | 重心距離 r_g | 積 |
|---|---|---|---|
| 分母（理論1RM仕事量） | 前腕0.016M + 手部0.006M = **0.022M** | 0.586L | 0.01289 M·L |
| 分子（実測トルク・`RIGHT_SEGMENTS`） | **前腕0.016M のみ**（手部なし） | 0.430L | 0.00688 M·L |

重力項で分母が1.87倍過大。

場所は分母が `compute_cycle_energy_elbow_wrist.py:273-282`、分子が `compute_torque_from_pose.py:47-54`。
§2-1 で分子に体幹＋頭の荷重が加わったが、腕の分節の重力項としての食い違いはそのまま残っている。

**決定（2026-09-23）: 分母に手を含める（現行どおり）。** 1RM はダンベルを手に持って肘を曲げる試技なので、
手も一緒に持ち上がる。一方、分子（プッシュアップ）の鎖は手をアームレストに置いた固定端とするので、手の重さは
アームレストが直接支え、手首・肘のトルクには入らない。両者の差は動作の違い（手が動くか固定か）であって、
定義の食い違いではない。腕を肩から吊る側の鎖（肩トルク、`--no-wrist-base` の肘）には、分母と同じく手を手首の
質点として入れた（`push_up_model.push_up_torques`）。分母の体節パラメータは `config` から取るようにした（値は同じ）。

### 2-3 慣性テンソルを回転させずグローバル系の ω に掛けている 【修正済み 2026-09-23】

`utils_dynamic.py:489`（numpy 経路）：`M = I @ dω + ω × (I @ ω)`。`I` は部位固定系の対角テンソル、
`ω` はグローバル系。`I_global = R I Rᵀ` の回転が無い。前腕の異方性は 2.2倍。
慣性項が全体の1.7〜4.6%なので最終影響は数%。

**対応**: リンクの向きに合わせて回す（`push_up_model.inertia_about_link`）。2 点のリンクからは軸まわりの向き
（ロール）が取れないので、横方向の 2 軸を平均した軸対称テンソルで近似する: I = I_t (E − uuᵀ) + I_l uuᵀ。
慣性係数表の上腕・前腕は横 2 軸の差が 4〜15% で、長軸（表の z）は 2〜6 倍小さい。3 経路とも同じ関数を通る。

### 2-6 手首の理論 1RM 仕事量で、ダンベルのてこの腕が 0 【修正済み 2026-09-23、7e589b2】

`compute_cycle_energy_elbow_wrist.py` の手首の分母は `r_x_wrist = 0.0`（「外力の作用点は手首」）なので、
ダンベルの項 `m_db × r_x` が消え、**手首の 1RM が分母に効いていない**（NaN かどうかの判定にしか使われない）。
分母は手の重さの項だけの 0.6〜2.1 J になる。

§5-1 で手首の軸が屈曲軸になり、手首の正の仕事を正しく拾うようになった（1 サイクル 2〜17 J）ので、
手首のスコアが 2〜30 と物理的にありえない値になった（以前は軸が別方向で仕事が小さく、0.5〜11 だった）。

手首の屈曲（リストカール）ではダンベルは手のひらにあり、手首の軸から手の重心程度離れている。
てこの腕を手の重心の距離（コードが手首の r_g に使っている「前腕長 × 0.506」の代用値）にした試算:

| 被験者 | 側 | 手首 1RM [kg] | 分母 今 [J] | 分母 試算 [J] | スコア 今 | スコア 試算 |
|---|---|---|---|---|---|---|
| 2 | L / R | 5.33 / 5.15 | 0.65 / 0.63 | 10.2 / 9.6 | 12.1 / 17.0 | 0.76 / 1.11 |
| 3 | L / R | 4.67 / 5.38 | 0.95 / 0.78 | 13.3 / 12.4 | 3.71 / 7.33 | 0.27 / 0.46 |
| 5 | L / R | 4.38 / 4.67 | 2.08 / 0.63 | 27.3 / 8.8 | 2.13 / 16.2 | 0.16 / 1.16 |
| 6 | L / R | 4.14 / 3.90 | 0.83 / 0.58 | 10.4 / 6.9 | 9.41 / 29.9 | 0.75 / 2.53 |
| 7 | L / R | 6.90 / 8.00 | 0.76 / 0.65 | 15.3 / 15.1 | 2.66 / 11.1 | 0.13 / 0.48 |
| 8 | L / R | 9.42 / 11.33 | 0.80 / 0.66 | 21.6 / 21.4 | 2.37 / 10.3 | 0.09 / 0.32 |
| 9 | L / R | 9.09 / 9.74 | 0.80 / 0.61 | 21.1 / 17.0 | 5.04 / 10.6 | 0.19 / 0.38 |

試算なら肘と同じ 0.1〜2.5 の範囲に入る。手の長さは実測していない（前腕長で代用している）。
**論文の定義（2026-09-23 に照合）**: 本文 4.5.1.2（53 ページ）が「手関節では rg = 0.506L であり、外力の作用点が
手関節そのものであるため rx = 0 とする。したがって手関節の理論仕事量には、推定 1RM に由来する項が入らない」と
明記している。コードは本文どおり。ただし本文の閾値の式 Sc = (0.415mx + c·mmax)/(0.415mx + mmax)（54 ページ）は
rx ≠ 0 が前提で、rx = 0 だと c に依らず Sc = 1 になり、手首の閾値 0.72 / 0.86 が意味を持たない（本文の中の矛盾）。
受領した論文系列CSVの手首分母は、1RM項を含む旧ゲージ式と7名全件で一致した（§6-6）。

**決定と対応（2026-09-23）**: 論文を絶対視せず、定義を直す。リストカールのダンベルは手のひらにあるので、
てこの腕を**手の中心**（手長 × 0.506）にする。手長は実測が無いので、前腕長から Drillis & Contini の体節長比
（手 0.108H、前腕 0.146H）で推定する（手長 = 前腕長 × 0.74）。上の試算の列（前腕長 × 0.506 で代用）より
てこの腕は 26% 短い。分母の計算は `compute_cycle_energy_elbow_wrist.theoretical_1rm_work` に括り出し、
`tests/test_theoretical_work.py` で固定した。肘の分母（前腕＋手、ダンベルは前腕長の位置）は変えていない。
新しい分母とスコアは §6-1。手首の 1RM に `wrist_*` と `wrist_*_inner` のどちらを使うかは保留（今は `wrist_*`）。

### 2-4 2点リンクの角速度は軸まわり回転を取れない（限界として明記）

`ω = (r × ṙ)/|r|²` はリンク軸まわりの回転（前腕の回内外）を常に0とする。
式の展開自体は正しい（`ω̇ = (r×r̈)/|r|² − 2(r·ṙ)(r×ṙ)/|r|⁴` を検算済み）。

### 2-5 加速度が位置の2階中心差分 → ノイズを約1800倍に増幅（限界として明記）

30 fps で dt² = 1.11×10⁻³。位置ノイズ1 mm が加速度 1.8 m/s²（g の18%）になる。

---

## §3 リポジトリの衛生と運用

### 3-1 壊れた別系統の出力が残っている 【リポジトリ外】

`output_data/torque/*_filtpos_est.csv` のトルクが異常：

| 被験者 | 肘 mean | 肘 max | 肩 max |
|---|---|---|---|
| 2 | 483 N·m | 2,465 | 5,106 |
| 3 | 664 N·m | **136,871** | **728,940** |
| 5 | 627 N·m | 34,022 | 32,101 |

生理学的上限（肘 50〜80 N·m）を桁で超えている。非m単位の位置を2階微分したためと見られる。
LPF系スコアの入力は別の `filtered_torque_lpf_recalc`。ただし0.5〜1.6 N·mという桁だけで妥当とは判断できない（§1-7）。
`out/00_README.md` が入力として挙げているので削除か明示的な隔離を推奨。

このリポジトリには `output_data/torque/` も `out/00_README.md` も無く、git 履歴にも無い。
**2026-09-23受領時の記録**: MANIFESTは被験者2・3・5・6・7・8・9の7件をWindowsの
`MAINCODE/output_data/_quarantine_filtpos_est/` に移しREADMEを置いたと報告している。
移動前一覧7件は受領したが、隔離先ファイル・README・移動後一覧はZIPに無い。隔離は報告済み、独立確認は未完了。

### 3-2 GUI から停止すると終了時の CSV が書かれない可能性が高い 【修正済み 2026-09-23、要実機確認】

`app/runners/worker.py:104-111` は「CSV の書き出しを待ちます」と表示してから `terminate()`（SIGTERM）を送るが、
`master_research_code.py` に SIGTERM ハンドラが無い。終了時にまとめて書く `kpts3d_*.csv` とトルク CSV は
書かれない可能性が高い。EKF 較正用の生 CSV は、これを避けるためにフレームごとに追記している
（`master_research_code.py:2839`）。

実機で確認し、必要ならハンドラでループを抜けるようにする。EKF とは独立に直すべき件。
EKF 設計メモの「スコープ外だが記録しておく別件」から移した。

補足: Windows の `QProcess.terminate()` は WM_CLOSE を送るだけで、コンソールのプロセスには届かない。
どちらの OS でもループの後の書き出しは走っていなかった。

**対応**: 停止ファイルで知らせる（`app/core/stop_request.py`）。`WorkerRunner` は実行ごとに一時ディレクトリを作って
環境変数 `APP_STOP_FILE` で渡し、停止時（計測のとき）はそこにファイルを置く。`master_research_code.py` は
メインループの**先頭**でそれを見て抜ける（途中に continue が多く、末尾の 'q' 判定に届かない周回がある）。
SIGTERM（Windows は SIGBREAK）も同じ停止要求として扱う。猶予 10 秒を過ぎれば従来どおり kill する。
`tests/test_stop_request.py` が子プロセスで「停止ファイル／SIGTERM でループを抜けて終了時の書き出しが走る」を
確かめる。実機での確認（GUI から止めて CSV が残るか）は残っている。

**再生での確認（2026-09-23）**: 本物の `master_research_code.py` を受け取った被験者 7 の動画で走らせ、ループに入って 15 秒で
停止ファイルを置いた（`tools/verify_run.py replay --stop-after-sec 15`）。`[STOP]` の後に kpts3d・aim_torque・gauge_energy が
書かれ、生 CSV と kpts3d は 496 行ずつ対応した。GUI の停止ボタンからの確認は、出力先の食い違い（§3-3）を直したうえで
実機で行う（`tools/verify_run.py check <出力先> --expect-stop`、`docs/usb_stereo_verification.md`）。

### 3-3 GUI の「出力先」と実際の出力先が違う 【修正済み 2026-09-23】

GUI は出力先を `~/Documents/WheelchairTorque` と表示していたが、計測は `config.save_dir` = 作業フォルダ相対の
`output_data` に書いていた。リポジトリから GUI を起動すると CSV はリポジトリの `output_data` に出るので、§3-2 の確認で
「CSV が無い」と見誤る（凍結アプリは作業フォルダが `~/Documents/WheelchairTorque` なので、その `output_data` に出ていた）。

**対応**: 置き場を `app.core.settings.measurement_output_dir()`（`~/Documents/WheelchairTorque/output_data`）の 1 か所で決め、
計測ページ・解析ページの表示と既定の入力フォルダ、ワーカーへの受け渡し（環境変数 `OUTPUT_DIR`、`config.save_dir` が読む）で
共有する。`OUTPUT_DIR` は GUI とワーカーの間の受け渡しで、設定画面の項目にはしない（名前を定数に入れ、スキーマの抽出器に
拾わせない）。GUI を通さずに起動したときは従来どおり `output_data`。`tests/test_output_dir.py`。
計測自身の録画（`cam*_output_*.mp4`）と `app.log` は作業フォルダのまま（§3-4）。

### 3-4 計測自身の録画は時間が詰まる 【記録のみ】

`master_research_code.py` の録画（`cam0_output_<ts>.mp4`）は、fps を `config.fps`（30）固定で書き、書き込みは間引きの
`continue` の後にしかない。4 Hz 間引きでは 8 フレームに 1 枚を 30 fps で書くので約 8 倍速になり、先頭の慣性の暖機 30 フレームは
等速なので時間軸も均一でない。writer は解像度の設定（`CAM_WIDTH` など）より前に最初のフレームの大きさで作るので、
解像度を変えると書き込みが黙って失敗しうる。作業フォルダに書くので、リポジトリから走らせると `video_io` の録画の組の
フォールバックに拾われる。再解析用の録画は `tools/record_stereo.py` で撮る（全フレーム、`frames.csv` に撮影時刻）。

---

## §4 コード品質（`/simplify` で見送った項目）

### 4-1 ネイティブ描画パスが既定で到達不能 【解消 2026-09-23】

`master_research_code.py:3976` が `DRAW_LABEL_CACHE`（既定1）を
`:3992` の `USE_NATIVE_DRAW`（既定1）より先に判定するため、DLL 一括描画アームに入らない。
commit 18aadcc が意図せず殺した。**どちらを既定にするか決める必要がある**ため未変更。
ネイティブ側は `py_native_overlay` を import できたときだけ有効になる（`:107-110`）。

**対応**: スプライトに一本化し、ネイティブの分岐と `USE_NATIVE_DRAW` を外した。ネイティブは DirectWrite の
Windows 専用（macOS では `ctypes.WinDLL` が無く import 自体が失敗する）で、フォントも Meiryo 固定で見た目が
OS ごとに変わる。18aadcc 以降は到達不能のまま使われていなかった。`py_native_overlay.py` と `native_overlay/` は
実行時参照が無くなった。引き継ぎ追加対応（d3ce0ee）で、両者の追跡ファイル（DLL・ビルド生成物を含む）と
唯一残っていた専用手動テスト `test/test_directwrite_overlay.py` を削除した。
設定スキーマは再生成した（`USE_NATIVE_DRAW` などが消えた）。

### 4-2 `_blit_label` / `_glyph_sprite` が `utils.put_text_jp` の二重実装 【解消 2026-09-23】

`master_research_code.py:2538` 以降の `_label_font` / `_glyph_sprite` / `_blit_label` が、
`utils.put_text_jp`（`utils.py:196`）と同一シグネチャで文字描画を再実装している。

フォントの取得とキャッシュは `app.core.resources.japanese_font` に集約済みで、`_label_font` もそこへ委ねている。
残る重複は描画と折り返し。折り返し規則は乖離したまま
（`utils.py:226` は常に `textwrap.fill`、`master_research_code.py:2581` は `len(text) > line_width` のときだけ）。

**修正案**：`_glyph_sprite`/`_blit_label` を `utils.py` へ移し `put_text_jp` の内部実装にする。
`DRAW_LABEL_CACHE` フラグと両方の分岐が消え、`master_research_code_00.py:477-486` と
`utils.py:286-292` の呼び出しも高速化される。
`put_text_jp` の「新しい配列を返す」契約を保つ必要があるため中規模。

**対応**: スプライト合成を `utils.draw_text_jp`（その場で描く）に移し、`put_text_jp` はコピーしてそれを呼ぶ
（グレースケールなどは従来の PIL 経路）。折り返しと行送りは PIL に合わせた（常に `textwrap.fill`、行送りは
「A の高さ + 4」）。キャリブレーション画面（`calib.py`、KNOWN_ISSUES に載っていなかった呼び出し元）の 2 行目の
ずれも直る。合成は四捨五入にした（PIL は MULDIV255）。`tests/test_text_drawing.py` が PIL で直接描いた画像との
一致（黒背景で完全一致、灰色で ±1）を固定する。`DRAW_LABEL_CACHE` は意味が無くなったので外した。

### 4-3 `LandmarkEKF` に2実装が恒久併存し、ベクトル化側が `cfg.h_fn` を黙って無視 【解消 2026-09-13】

- 逐次版とベクトル化版の一致を固定するテストを追加（`tests/test_landmark_ekf.py`、c792b41）。
  外れ値・欠測・dt 2 通りで相対差 1e-12 以内
- `extended_kalman_filter.py` へ移設（feef20a）
- `LandmarkEKF` に `h_fn` / `h_jac_fn` を渡すと生成時に例外（`extended_kalman_filter.py:248-252`）。
  `ExtendedKalman1D` で片側だけ渡すのも例外（b3b76e1）
- あわせて、NumPy 2 で逐次側（`EKF_VECTORIZED=0`）が `TypeError` で動いていなかったのを直した（b3b76e1）

一本化はしていない。系列別の雑音パラメータ（55511e1）も両実装に入れ、食い違いは一致テストで検出する形にした。

### 4-4 その他（小）

- `t_u_accum` / `t_u_fb` / `t_u_hist` / `t_u_cyc` / `t_u_gauge`（`master_research_code.py:3594-3945`）は、
  周囲の慣例（`t_seg` の使い回し）と不一致。区間は連続しているので統一可能
- `os.getenv` が `master_research_code.py` に 148 箇所。アプリ側は型付き設定（436d564）から環境変数を組み立てて
  渡すようになったが、スクリプト側の読み取りは従来のまま。`config.py` に `env_flag(name, default)` を置く余地がある

**対応（2026-09-23）**:

- 計時変数（`t_u_wait` を含む 6 組）を `t_seg` に揃えた
- `config.env_flag` を置き、`in ('1','true','True')` 型 32 か所と `not in ('0','false','False')` 型 11 か所、
  「真でない」型 1 か所を置き換えた。前者と後者は `''`・`'yes'`・`'TRUE'` で結果が逆になっていた
- スキーマの抽出器が `env_flag` を bool として拾うようにした。`not in` 型は str と推定されていたので、
  `DEBUG_LOGS`・`GRAB_PARALLEL` など 12 項目が GUI でテキスト欄になっていた（保存済みの "1" はそのまま読める）
- 追加対応（13a34b1）: 上記の残り7か所と `E_FC_ADAPTIVE_ON`（整数として読んでいたオン・オフ）も統一した。
  ASTで配線と既定値を確認し、スキーマを再生成した。

---

## §5 力学モデルの残件（計画メモから持ち越し）

### 5-1 手首の基準軸が肘の屈曲軸のまま（A-4 (1)） 【修正済み 2026-09-23】

リアルタイムは `links["wrist_R"]` が前腕、`parent_links["wrist_R"]` が上腕
（`master_research_code.py:3543-3576`）で、`y = 親 × z` は肘の屈曲軸になる。
手首のグローバルトルク 149 N·m のうち `wrist_R_local_y` は 6.4 N·m（4%）しか拾えていなかった。

オフラインはさらに悪い。手首基点の鎖の先頭 `forearm_R_wrist` は n=0 なので親が無く
（`compute_torque_from_pose.py:453-456`）、グローバル軸との外積で軸が決まる。
受け口の `root_parent_vec` はあるが `main()` から渡されていない。

手の点（17〜20）は 8e20586 で `pose_keypoints` に入ったが、まだどこからも使われていない。
人差し指と小指の中点を中手骨頭の代わりにし、「リンク＝手、親＝前腕」にする。
対象はリアルタイム、スマホ経路（`app/runners/network_measure.py` の `TORQUE_LINKS` / `PARENT_OF`）、オフラインの 3 か所。

**スコアに入れるには手の点の入った姿勢データが要る**（「データの所在確認」を参照）。

**対応**: 3 経路とも `push_up_model.joint_axes` で局所軸を作る。手首は link = 前腕（手首 → 肘）、
parent = 手（手の点 → 手首）で、y は前腕と手に直交する屈曲軸。手の点が無い・手首がまっすぐに近い
（前腕と手のなす角の sin が 0.25 未満）フレームでは parent = 肘 − 肩 とし、y を肘と同じ屈曲軸にする。
手を固定端として前腕が回る面は腕の面なので、手のひらの向きが分からなくても屈曲の成分はこの軸で取れる。

**スコアへの影響**: 被験者 3・5・6・7・8 の姿勢データには手の点が無いので、スコアの手首は「肘と同じ屈曲軸」で
出る。以前の軸（前腕と全体座標の基準軸）は、前腕が鉛直に近いと局所 y が水平の面内方向を向き、腕の面内で
前腕が倒れる動き（手首の屈曲そのもの）の仕事が 0 になっていた（`tests/test_cycle_energy_angles.py::TestWristAxis`）。
手首の仕事が正しく出るようになった結果、分母の欠陥（§2-6）が表に出た。

### 5-2 仕事率の ω が τ の軸と揃っていない（A-4 (2)、H-B） 【修正済み 2026-09-13】

修正前:

- **リアルタイム・スマホ経路**: `P = τ·ω`（全体座標の内積）で、ω は各部位の**絶対**角速度。
  肘角を保ったまま腕を振るだけで仕事が出ていた（合成データで 1 サイクル 7.6〜280 J）
- **スコア**: τ_y に、軸の作り方が別の角速度を掛けていた（肘は「+Y まわりの肘角」、手首は「水平面からの前腕の傾き」の微分）。
  鏡映すると τ と ω の片方だけが符号を変える。被験者 3 を左右反転すると、左の W_pos（301.94 J）が右の W_neg と一致した。
  **左右で逆の相（押し出しと戻し）を積算していた**

修正: 3 経路とも `utils.compute_joint_power` で `P = τ_y × (ω_リンク − ω_親)·y` を求める。
y 軸は局所トルクと同じ作り方（`compute_local_torque`）なので、鏡映で τ_y と ω_y が揃って符号を変え、P は変わらない。

| 経路 | リンク側 − 親 | 場所 |
|---|---|---|
| スコア（`--wrist-base` の鎖） | 手首: 前腕 − 手（固定）、肘: 上腕 − 前腕 | `compute_cycle_energy_elbow_wrist.py` の `_joint_powers` |
| リアルタイム・スマホ | `OMEGA_SOURCE` の部位 − `PARENT_OF` の関節の部位 | `master_research_code.py` の `parent_key_of`、`network_measure.py` の `_accumulate_power` |

スコアはトルク CSV の `*_local_y` 列ではなく、全体座標の列（`elbow_R_x` など）と姿勢から求めるようになった
（`recalc_elbow_local_torque.py` の局所列はスコアに使われない）。角度を経由しないので、
§1-1（fps の掛け戻し）・§1-2（折り返し）の誤りは構造的に起きない。

テスト: `tests/test_joint_power.py`（手計算の値と鏡映）、`tests/test_cycle_energy_angles.py`
（体幹を持ち上げたときの肘の正の仕事 = 位置エネルギーの増加 30.56 J、鏡映した動きで左右一致）、
`tests/test_network_measure.py::TestJointPower`（腕を一体で回しても肘の仕事は 0）。
`master_research_code.py` は import できないので、スマホ経路と同じ式で書いてある。

残り: 手首の局所軸はまだ全体座標の基準軸から作っている（§5-1）。リアルタイム経路は、
リンク側と親の対応そのものが関節ラベルと合っていない疑いがある（§5-7）。

### 5-3 左右差（E-1） 【切り分け済み 2026-09-13】

骨格を左右反転（鏡映）した動きを流し、元の右と反転後の左を比べた。コードが対称ならデータに依らず必ず一致するので、
コードが作る左右差とデータにある左右差を分けられる。

| 候補 | 結果 | 対応 |
|---|---|---|
| E-1a 左右で同じ符号条件 | **問題なし**。トルクも、親リンクとの外積で作る y 軸も擬ベクトルなので、`τ·y` は鏡映で変わらない。計画メモの「左右で逆符号が正常」はトルクを普通のベクトルとして扱った誤り | 変更なし（H-D は不要になった） |
| E-1b 右肩だけ上胴体の ω・ω̇ を反転 | **非対称**（手首・肘に約 1%）。`utils_dynamic.py` と `master_research_code.py` のネイティブ経路に複製されていた | 修正。補正を `utils_dynamic.trunk_segment_inputs` に一本化 |
| E-1c 肩の質量配分 A0/A1 | **問題なし**。右肩の分担 = 左手首までの距離 /（左＋右）はテコの分配として対称 | 変更なし |
| E-1d 慣性用のリンク長を左腕と右脚だけから取る | **非対称**（小） | 修正。腕と大腿の慣性テンソルを左右別に持つ |
| E-1f（新規）上胴体の関節位置 | **非対称**（肩トルクで 170%）。両チェーンとも `both_shoulder` の始点（左肩）まわりで計算していた | 修正。`calculate_individual_torques(p1s=...)` でチェーン側の肩・腰を渡す |
| スコア経路の τ と ω の軸の食い違い | **非対称**（左右で逆の相を積算） | 修正（§5-2） |

テストは `tests/test_left_right_symmetry.py`（鏡映で |τ| が入れ替わる、local_y の符号が保たれる、上胴体の M が左右で同じ）。

3 件を直しても、被験者 3 の実データの左右差はほぼ変わらない（スマホ経路で手首 |τ| 中央値 209 vs 304 N·m）。
**残る左右差はデータ由来**（体がカメラに対して斜め、など）。計画時の 71.8 vs 11.2 と 6a9ffa2 の 53.3 vs 74.8 は、
§1-5 の重力の向きの誤りも含んでいる。

### 5-4 ローカル座標系を作れないとグローバル値をそのまま返す（E-4、H-C） 【決定・修正済み 2026-09-23】

`utils.compute_local_torque` の早期リターン（`utils.py:334, 337, 376, 381`）で、リンクが非 finite・退化・
参照軸が全滅したとき、`torque_global` がそのまま `_local_*` 列に出る。
100 N·m 級のグローバル値がローカル列に紛れてスコアに入り得る。
オフラインの `compute_torque_from_pose.py:456` と `recalc_elbow_local_torque.py:98`、
スコアの仕事率（`utils.compute_joint_power`）もこの関数で軸を作る。

**決定と対応**: 値は今のまま返し、`utils.LocalFrameFallbackWarning` を出す（Python の既定では呼び出し箇所ごとに
1 回だけ表示）。オフラインは件数を数えて `*_meta.json` の `local_frame_fallbacks` に残す。実データ 10 本では
0 件だった。実際に起きうるのはリンクが非有限・長さ 0 のときだけ（直交する 3 軸のうち 2 本と同時に
|cos| ≥ 0.95 にはなれないので、基準軸が全滅する分岐には来ない）。

### 5-5 定義が使われていない／出典が不明（E-3）

- `config.COM_FRACTIONS["hand"]`（0.506）と `config.m3`（手の質量）は参照ゼロ。§5-1 で使えるようになる
- 慣性係数 CSV の手の行（k=5）を渡している箇所が無い。手の慣性テンソルは一度も計算されていない
- 大腿の COM 比 0.433（`config.py:201`）の出典が未確認

**確認（2026-09-23）**:

- 大腿の 0.433 は Winter の表の値（近位 0.433 / 遠位 0.567）。`config.py` に出典を書いた
- 手の慣性（行 5）は使わない。プッシュアップの鎖では手が固定端、腕を吊る鎖では手首の質点
  （m L²/12 ≈ 2×10⁻⁴ kg·m² で前腕の 1/5 程度）。理由を `config.py` に書いた
- `COM_FRACTIONS["hand"]` と手の質量比は、スコアの分母が `config` から取るようになって使われるようになった
- **新たに分かったこと**: 上腕の質量比 0.0227 は Winter（0.028）ではない。`config.SEGMENT_MASS_FRACTIONS` の
  「Winter の表」という説明と食い違う。`config.m1` と慣性回帰式の行に合わせた値で、出典は不明のまま

**出典の追加調査（2026-09-23）**: 初期コミット `765e962` の `config.py` にも 0.0227 があり、
直前のコメントが [SPORTS SENSING の解説・実装](https://note.com/ss_sports_lab/n/nd284cb2c3628) を参照している。
ただし、同ページで公開されている `RUArm` / `LUArm` の質量比は **0.027**。
0.0227 を採った理由は履歴にも書かれておらず、当該URLを0.0227の出典とはできない。
同ページの参考文献は [阿江・湯・横井（1992）](https://www.jstage.jst.go.jp/article/biomechanisms/11/0/11_KJ00004275252/_article/-char/ja/)。
誤記か別資料由来かは確定できないため、0.0227を変更せず、原資料確認を残す。

### 5-6 出力ファイルに規約の版が無い（D-3） 【修正済み 2026-09-23】

2026-09-08 に左右ラベルが入れ替わり（R-1）、§5-1・§5-2 で `*_local_y` の意味も変わる。ところが
`forearm_R_{timestamp}.npy` などのファイル名にも、`compute_torque_from_pose.py` の `*_meta.json` にも規約の版が無い。
`config.py` に `LANDMARK_SCHEMA_VERSION` を置き、npy の名前と meta に入れる。既存ファイルは触らない（タグ無し＝v1）。

**対応**: トルクの意味を含むので名前は `config.OUTPUT_SCHEMA_VERSION`（= 2）にした。版ごとの変更点も同じ場所に
書いた。オフラインは `*_meta.json` の `schema_version`、USB 経路はトルク・エネルギーを含む出力
（`aim_torque_vec_*`、`cycle_energy_debug_*`、`forearm_*`・`tau_wrist_*` の npy）の名前に `_s2` を付ける。

### 5-7 リアルタイム経路の関節ラベルと基準点が合っていない疑い 【修正済み 2026-09-23】

チェーンの部位は `[上腕, 前腕, 上胴体, 下胴体, 大腿]` の順で、関節トルクは部位 j の `p1`（リンクの始点）まわりに
j 以降の部位を足して求める（`utils_dynamic.calculate_individual_torques`）。`p1` は `part_calculations` の start なので:

| ラベル | 計算しているもの |
|---|---|
| `wrist_R` | 右**肘**まわり、全部位 |
| `elbow_R` | 右**手首**まわり、前腕・胴体・大腿（上腕を含まない） |
| `shoulder_R` | 右肩まわり（E-1f の修正後）、胴体・大腿 |

一方、局所軸（`links` / `parent_links`）と角速度（`omega_map`）はラベルどおり（`wrist_R` = 前腕リンク）に作っている。
意図した鎖の向きを確認し、部位の並びかラベルを揃える。§5-2 の相対角速度の向きもこれに依存する。
過去の検証（再検算 R-1）はリンクの左右ラベルを直したが、この対応は扱っていない。

**対応**: USB・スマホ経路とも `push_up_model.push_up_torques` を呼ぶ。手首 = 前腕（関節 = 手首）から、
肘 = 上腕（関節 = 肘）から先を足す鎖で、`wrist_R` は右手首まわり、`elbow_R` は右肘まわり。局所軸と仕事率の
相対角速度も同じモジュールで作る（手首 = 前腕、手は固定／肘 = 上腕 − 前腕／肩 = 上腕 − 上胴体）。
USB 経路の手首のゲージは「局所 y の正の部分の時間積分 ÷ 50」をやめ、正の仕事 ∫max(P, 0)dt [J] にした。
`tests/test_path_consistency.py` が、静止姿勢でスマホ経路とオフラインの 6 関節の局所トルクが一致することと、
USB 経路が同じ関数を呼んでいること（AST）を固定する。

### 5-8 リアルタイム経路に体幹荷重のモデル（§2-1）が入っていない 【修正済み 2026-09-23】

§2-1 のモデル（手を固定端に、肩へ体幹＋頭を載せる）はオフライン経路（`--wrist-base`）にだけ入っている。
リアルタイム経路とスマホ経路は下胴体（Imode 4）に体重 60 kg を丸ごと渡しており、被験者 3 の骨格で
手首・肘・肩の |τ| 中央値が 200〜300 N·m になる（生理学的な目安の 3〜4 倍）。

**対応**: §5-7 と同じ。鎖から上胴体・下胴体・大腿と地面反力の簡易モデル（大腿の角度で f_E を付ける）を外し、
体幹＋頭の荷重（片腕 17.34 kg）を肩に載せる。胴体・大腿の慣性テンソルも作らなくなった。

---

## §6 検証と運用

### 6-1 スコアの通し再計算（D-1） 【実施 2026-09-23。集計範囲は判断待ち】

初回は中間データが未受領だったため、`Adjusted 3D Pose/*.csv` から回し直した。**実際に通る手順**（論文の `*_lpf.csv` を
作っていたのは `tmp_filter_pose_torque.py` の 2 Hz 固定。2026-09-23 に前処理を `pose_lowpass.py` に替えた）:

```
Adjusted 3D Pose/*.csv
  → pose_lowpass.py --in-dir "Adjusted 3D Pose" --out-dir output_data/filtered_pose_lpf
      （Butterworth 4 次、filtfilt。fc = clip(6·f0, 2.1, 6.0)、SNR < 3 dB なら 2 Hz）   *_lpf.csv と *_lpf_meta.json
  → compute_torque_from_pose.py（既定のモデル。cm の 2 本は --pos-scale 0.01）   *_torque.csv
  → auto_detect_cycles_pose_ranges.py --pose-dir                     *_with_cycles.csv（範囲表にある 7 本だけ）
  → 1RM の表（m_max_all_merged.csv。肘は elbow_*_outer、手首は wrist_*。§6-4）
  → compute_cycle_energy_elbow_wrist.py                              スコア
```

`filter_pose3d.py`（関節ごとに 2.5〜4 Hz）は出力の列に `_f` が付き、ファイル名が `*_filt.csv` になる。
`compute_torque_from_pose.py` と `auto_detect_cycles_pose_ranges.py` は `_f` の列も読めるが、`compute_cycle_energy_elbow_wrist.py` は
`joint_{id}_x` の形しか読めず、範囲表の名前とも合わない（以前ここに「後段が読めない」とだけ書いていたのを正確にした）。

**f0 から決めたカットオフ（2026-09-23）**: 8 本は f0 = 0.352 Hz（Welch の刻み 0.117 Hz の 3 つ目）で fc = 2.11 Hz、
被験者 2 は f0 = 0.469 Hz で 2.81 Hz、4_0 は 0.703 Hz で 4.22 Hz。論文の表 5 は被験者 8 を 5.63 Hz（f0 = 0.938 Hz）と
しているが、今回の被験者 8 は 2.11 Hz で食い違う（表 5 の元の計算は未確認）。

`recalc_elbow_local_torque.py` はスコアに効かない（スコアは全体座標の列から局所軸を作り直す）ので省いた。
軸の作り方は `compute_torque_from_pose.py` と同じにしたので、通しても値は変わらない。
実行したスクリプトと出力はセッションのスクラッチに置いた（リポジトリには書いていない）。

**初回結果（受領前の仮値。受領outerでの更新は下記）**: サイクルごとの `ratio_pos_vs_1rm` の中央値。**1RM の肘は `elbow_*`（§6-4）、手首は分母が壊れている（§2-6）**。

| 被験者 | サイクル | 肘 R | 肘 L | 手首 R | 手首 L |
|---|---|---|---|---|---|
| 2 | 8 | 0.72 | 0.99 | 16.96 | 12.06 |
| 3 | 10 | 0.21 | 0.65 | 7.33 | 3.71 |
| 5 | 7 | 0.25 | 0.63 | 16.23 | 2.13 |
| 6 | 14 | 2.09 | 1.22 | 29.92 | 9.41 |
| 7 | 13 | 0.21 | 0.27 | 11.08 | 2.66 |
| 8 | 21 | 0.27 | 0.40 | 10.26 | 2.37 |
| 9 | 8 | 0.63 | 0.60 | 10.59 | 5.04 |

被験者 3・5・6・7・8 の肘（両側）: 中央値の平均 0.62、中央値 0.33（旧コードに倍率 1 を与えた場合は 0.90 / 0.62）。
被験者 5 は `5_1stereo_pose_scaled.csv`（z が上、左前腕 0.68 m・大腿 0.10 m とデータの質が怪しい）。

**集計の範囲（判断待ち）**:

- 3_1・4_0・5_stereo はサイクル検出の範囲表（`auto_detect_cycles_pose_ranges.py`）に名前が無く、`_with_cycles` が作られない
- 被験者 4 は `compute_cycle_energy_elbow_wrist.py` で除外している。理由は論文にある（57・65 ページ: 左手関節の保護の
  ため課題中に意図的に力を加減した）。ノイズ評価（図 17）は本文では被験者 4 を含む 8 名だが、
  ノイズ計算の固定除外は追加対応（9a72f2c）で解除済み。ただし4のサイクル範囲は未確定で、8名の再集計は未実施
- 論文はスコアを右側（右肘・右手首）だけ報告している。上の表は左右とも出している
- 前処理を `filter_pose3d.py`（関節ごとに 2.5〜4 Hz）にすると、スコアが 10〜30% 動く（2 Hz 固定と比べて。今の前処理は上の f0 方式）

**Windows受領後の仮再計算（同日）**: 受領LPF姿勢と `m_max_all_merged.csv` のouter列で同じ7試技を再計算した。
前処理・範囲・モデル・手首分母・axis重力は維持。中央値は次のとおり。

| 被験者 | 肘R | 肘L | 手首R | 手首L |
|---|---:|---:|---:|---:|
| 2 | 0.620 | 0.827 | 16.959 | 12.060 |
| 3 | 0.181 | 0.489 | 7.327 | 3.714 |
| 5 | 0.154 | 0.176 | 16.227 | 2.134 |
| 6 | 1.288 | 0.869 | 29.923 | 9.414 |
| 7 | 0.195 | 0.239 | 11.075 | 2.660 |
| 8 | 0.227 | 0.316 | 10.264 | 2.373 |
| 9 | 0.515 | 0.555 | 10.585 | 5.040 |

原本・既存出力を上書きせず、`~/Downloads/transfer_20260923_review/current_provisional/` に保存。
全metaで手首の `hand=0`。手の点の不足、分母・集計条件の判断は残る。
論文0.99のGCVSPL系列とは試技・サイクルが違うため、これを論文の最終改訂値として使わない。
詳細と旧仮値との比較は [転送照合](docs/windows-transfer-audit-2026-09-23.md)。

**定義の修正後の再計算（2026-09-23、最新）**: 前処理を f0 のカットオフ（`pose_lowpass.py`）、手首の分母を手の中心（§2-6）、
肘の 1RM を outer（§6-4）にして、同じ 7 試技を回した。重力は `axis`、範囲表・被験者 4 の除外は従来どおり。
サイクルごとの `ratio_pos_vs_1rm` の中央値。出力はセッションのスクラッチ（リポジトリの外）。

| 被験者 | サイクル | 肘R | 肘L | 手首R | 手首L | 分母 肘R / 手首R [J] |
|---|---:|---:|---:|---:|---:|---|
| 2 | 8 | 0.651 | 0.834 | 1.494 | 1.071 | 40.6 / 7.1 |
| 3 | 10 | 0.174 | 0.496 | 0.604 | 0.362 | 46.0 / 9.2 |
| 5 | 7 | 0.156 | 0.179 | 1.658 | 0.226 | 65.6 / 6.5 |
| 6 | 14 | 1.288 | 0.875 | 3.458 | 1.018 | 32.3 / 5.1 |
| 7 | 13 | 0.192 | 0.242 | 0.663 | 0.182 | 66.1 / 11.2 |
| 8 | 21 | 0.226 | 0.316 | 0.432 | 0.119 | 75.3 / 15.9 |
| 9 | 8 | 0.529 | 0.560 | 0.524 | 0.262 | 41.0 / 12.6 |

- 手首の分母は 0.6〜2.1 J から 5〜16 J になり、手首のスコアは 0.12〜3.46 と肘と同じ桁に入った
- 被験者 3・5・6・7・8 の両側: 肘は中央値の平均 0.414・中央値 0.234、手首は平均 0.872・中央値 0.518
- 論文と同じ集計（右肘・右手首、7 名）: 平均 0.861、範囲 0.156〜3.458（論文の GCVSPL 系列 0.99 とは試技・前処理が違う）

### 6-2 実機確認（D-2）

R-1〜R-4・R-6 は表示に効く。USB 経路（`master_research_code.py`）とスマホ経路（`app/runners/network_measure.py`）の
両方で、ゲージの値が生理学的に妥当な範囲に入るか、16 点になった三角測量と EKF が 30 fps で回るかを見る。
§3-2 と §6-3 の S6 も同じ機会に行う。

**道具（2026-09-23）**: USB 2 台の全フレームを録画する `tools/record_stereo.py`、録画を計測に読み込ませる
`tools/verify_run.py replay`、出力フォルダを確かめる `tools/verify_run.py check`。手順は `docs/usb_stereo_verification.md`。
ゲージの値と閾値は HEADLESS でも `gauge_energy_<ts>_s2_g*.csv` と同名の `.json` に残るようにした（§6-8）。

**再生での試走（受け取った被験者 7 の動画、Mac M1、HEADLESS）**:

- 処理の速さは間引きなしで 35 fps、4 Hz 間引きで 31 fps（ファイル入力なのでカメラ待ちが無い上限。ライブの 30 fps は実機で）
- 骨長は上腕 0.28/0.31 m・前腕 0.24/0.27 m、重力は Z+ が上（体幹の傾き 9°）
- トルク |τ_y| の 95 パーセンタイル（間引きなし）: 右手首 24.6、右肘 7.5、左手首 50.8、左肘 13.3 N·m（見込みの 10〜40 N·m 台）。
  中央値が 0 なのは、GUI の既定 `RT_DYN_ON_RISE_ONLY=1` が立ち上がりを検出するまで τ を 0 にするため
- **間引きなしではサイクルを 1 回も検出しない**（§6-9）。ゲージの帯（約 95〜128 J）には一度も届かない（§6-8）
- 受け取った校正は c0 の fx = 1631・fy = 1169（c1 は 1422・1229）と縦横で 3〜4 割違い、歪み係数も k2 = −668 と極端
  （歪みは読んでから捨てている）。4:3 の画像で校正して 16:9 に引き伸ばしたような値。骨長には目立って効いていない
- 本体は `CAM_WIDTH`・`CAM_HEIGHT` が無いとカメラの既定の解像度で開く。校正（`calib.py`）は 1280×720 に設定するので、
  ライブ計測では同じ解像度になっているかを起動ログの `[CAMDIAG]` で確かめる
- Windows では、校正（`calib.py` の `app.core.video_source.open_capture`: DSHOW → MSMF の順）と計測（`video_io.open_capture_and_read_first`: 番号には CAP_ANY = MSMF を先に試す）でカメラの開き方が違う。露出の値の意味や、2 台のときのデバイス番号の並びが変わりうる（校正の cam0 が計測の cam1 になるなど）。録画ツールは計測に合わせ、`meta.json` にバックエンド名を残す。実機で両者の番号と物理カメラの対応を確かめる
- 閾値の計算（§6-8）をモジュール直下へ移したので、シェルから直接起動して `BODY_MASS_KG`・`G_SCALAR` に数値でない値を渡すと起動時に落ちる（以前はゲージがあるときだけ try の中で読んでいた）。GUI は数値を渡すので起きない。直していない

### 6-3 EKF の自己チューニング

設計メモの S1〜S5・S7・S8 は実装済み（d4b3a79〜93f1723）。2026-09-23 に S9〜S11 のうち実機の要らない部分を入れた:

- **S9 配線**（`app.tuning.ekf_profile.runtime_noise`）: `EKF_PROFILE` が解決できればプロファイルが勝つ。
  指定が無ければ環境変数のスカラー（今までの挙動）、dt が合わない・BPF が有効なら同梱既定値。系列は ID の昇順。
  EKF の生成を `_DYN_DT` の後に移し、BPF の `fs = 1/_DYN_DT`。起動ログとサイドカーに出どころ（`ekf_noise`）を残す。
  体格の比 L_run / L_cal は、肩と肘が三角測量できた先頭 30 フレームの肩–肘の中央値で決めて掛け直し、
  人体としてありえなければ計測を止める（CSV は書き出し、終了コード 3）。比と長さはサイドカーに追記する。
  ディレクトリからは `ekf_profile_*.json` だけを探す（生 CSV のサイドカーを拾っていた）。step の広い except を狭めた
- **S10 ロバスト更新**（`robust_gate`、`EKF_ROBUST_GATE` 既定オン）: 門の外は S' = y²/c²（r' = S' − P00）で取り込む。
  設計メモの「等価に r' = r·(|y|/(c√S))²」は P00 = 0 のときしか等価でないので、S' = y²/c² の方に揃えた。
  合成データ（振幅 0.1 m・0.5 Hz の往復、観測雑音 5 mm、較正値）で、捨てる方式は追従を失い誤差 RMS 1.08 m、
  ロバスト更新は 5.5 mm。欠測の上限 `EKF_MAX_GAP_S` の仕組みも入れたが、**既定は 0（無制限）**。NaN が下流へ流れる
  ので、既定値は S6 の欠測長の分布で決める。サイクルの基準値だけは NaN を避けるようにした
- **S11 GUI**: 解析ページに「EKF の較正プロファイルを作る」（`app.runners.tune_ekf`）

残りは **S6（実機での収録。間引きなしと 4Hz 間引きの 2 設定）** → S9b（1 試技で RMS 差と棄却率が期待範囲に入るかの
受け入れ判定）と、同梱既定値・`EKF_MAX_GAP_S` の既定値の差し替え。スマホ経路は EKF を使っていない（設計どおり）。

**S6 の道具と試走（2026-09-23）**: 録画を 2 設定で再生すれば、同じ試技で「間引きなし」と「4 Hz 間引き」を比べられる
（ファイル入力は処理が遅くても dt が正しい）。S9b の材料（系列ごとの RMS 差と、実行時の雑音パラメータで数えた棄却率）は
`tools/verify_run.py check` が出す（それまで計算するコードが無かった）。受け取った被験者 7 の動画（60 秒）で試走した結果:

- 間引きなし（1819 フレーム）・4 Hz（253 フレーム）とも、推定できた系列はほぼすべて探索範囲の端に張り付き（●）、
  ρ1 は 0.77〜0.99。設計メモの中止条件（端に張り付く系列が過半数、または |ρ1| > 0.3）に当たる
- 約 4 割の系列は q_acc の初期値が取れない（どの間隔でも過程の寄与が観測誤差を上回らない）。`ekf_estimate` の表は
  これを「推定不能（有限値が足りない）」と表示するが、生 CSV は全系列 100% 有限で、文言が実際の理由と違う
- 4 Hz は n_eff が 300 に届かない（60 秒では足りない。設計メモ :275 のとおり 80 秒以上要る）
- どちらのプロファイルも全 48 系列が同梱既定値（仮置き）になった。それを付けて再生すると RMS 差の中央値 37 mm・最大 289 mm、
  棄却率の中央値 17%・最大 81%（環境変数のスカラーでは中央値 26 mm・0.6%）。脚（25〜28）が最も大きい
- `tune_ekf --out` にフォルダを渡すと `IsADirectoryError` で落ちる（ファイルのパスを取る。既定は生 CSV の隣）
- 古い構成（Windows、mp4、fx≠fy の校正）の映像なので、判断は新しい構成で `tools/record_stereo.py` の録画（MJPG）を使ってやり直す

### 6-4 1RM の `elbow_*_outer` 列の出どころ 【決定 2026-09-23、元シートの列は未確定】

**決定（2026-09-23）**: `elbow_*_outer` は「肘が内側へ曲がろうとするトルクを打ち消す向き」の筋力、つまり肘の伸展の力を
測った値。プッシュアップで肘がするのも伸展なので、肘のスコアの分母にはこれを使う（`ONE_RM_COLUMNS`、7e589b2）。
値は受領した `m_max_all_merged.csv` から取る（リポジトリ直下に写した。`*.csv` は追跡外）。以下は経緯。


スコアは 1RM を `m_max_all_merged.csv` の `elbow_{R,L}_outer` と `wrist_{R,L}` から読む。ところが
`m_max_all_builder.py` が作るのは `subject_id, wrist_L, wrist_R, elbow_L, elbow_R` の 5 列だけで、`*_outer` を作る
手段がリポジトリに無い。`m_max_all_merged.csv` は2026-09-23に受領し、対象者のouter値は回収できた。元のスプレッドシートは「回数／重さ」の組が
10 組あり、builder はそのうち 4 組しか使っていない。どれが「elbow outer」かはシートからもリポジトリからも決められない。
§6-1の初回は仮に `elbow_*` を使い、受領後はouter列で別フォルダに再計算した。元シート列の対応は未確定。あわせて `m_max_part_5.json` の elbow_R（5.385）がシートと `m_max_all.csv`
（11.111）と食い違っている。

### 6-5 その他の小さな発見（2026-09-23）

- `estimate_joint_torque_from_pose.py` は `compute_torque_from_pose.build_output` を古い引数で呼んでおり、
  動かなかった。**追加対応で修正済み（9a72f2c）**: 部位→関節の共有対応表で辞書を組み立てる。
  旧CLIの手首簡易推定・支持荷重式は維持する互換用で、論文再計算には使用しない。
  論文再計算は `compute_torque_from_pose.py` を使う
- `auto_detect_cycles_pose_ranges.py` の `_detect_in_range` は、範囲外のフレームしか無いと 2 値しか返さず
  `ValueError` で落ちていた。**追加対応で修正済み（9a72f2c）**: 常に5値を返し、全行の `cycle_index=-1` を保存する
- `plot_ratio_vs_subject.py` は 3_1 のスコアを期待しているが、今の範囲表では作られない
- 2_stereo と kpts3d_9 は cm 単位。kpts3d_9 は全関節の |値| の中央値が 44.5 で、単位の自動判定（50 で mm）に近い
- レビューで直したもの: 手首の手のひらの軸を肘の屈曲軸と同じ向きに揃えた（手の前後で τ_y の符号が跳んでいた）、
  USB 経路のサイクルの基準値を集まった数で割る（欠測で縮んでいた）、手首の記録（OFFLINE_WRIST_CAPTURE）の前腕を
  読み手の規約（肘→手首）に戻した、2 回目の SIGTERM で止まる、停止用の一時ディレクトリを計測のときだけ作り
  起動に失敗しても残さない、`--wrist-base` でダンベルを指定したら警告する、スコアの局所軸にも重力の逆向きを渡す、
  スマホ経路で腰が一度も取れなくても落ちない
- `tools/verify_dynamics_recheck.py` は名前表に手の点（8e20586 で追加）が無く、関節索引の節で `KeyError: 17` で
  止まっていた。名前を足して直した（全節 OK）

### 6-6 論文本文との照合（2026-09-23）

修士論文の最終版（`~/master/村山_260831_final.pdf`、133 ページ）と計算を突き合わせた。本文は編集していない。

**従来調べたコードと本文の食い違い**（LPF系と今回回収したGCVSPL系は区別する。下の追記を参照）:

| 本文 | 計算（論文時点のコード） |
|---|---|
| 49 ページ「加速度・角加速度の項を無視した簡約形を実装した」 | 慣性項（ω̇、重心加速度）を計算に入れている（寄与 1.7〜4.6%） |
| 47 ページ「角度に GCVSPL を当て、2π の折り返しを処理した」 | 2 Hz の Butterworth（`tmp_filter_pose_torque.py`）、`np.gradient × fps`（30 倍、§1-1）、unwrap なし（§1-2） |
| フィルタは 6·f0（2.11 Hz、被験者 8 は 5.63 Hz、表 5） | 2 Hz 固定の Butterworth ＋ Savitzky–Golay |
| 重力の向き・座標軸の記述なし | −z（奥行き）に置いていた（§1-5） |
| 図 7 に体幹の荷重（mg）を描いている | 既定では体幹の荷重を入れていない（`--wrist-base` が既定オフ、§2-1） |
| 前腕の I_xx = 74.33×10⁻⁴（51 ページ）と実測前腕長 0.19〜0.26 m（53 ページ） | 74.33 は前腕長 0.30 m の値。0.19〜0.26 m なら 12.6〜51.9 ×10⁻⁴（§1-4） |
| 表 2 の閾値 0.72 / 0.86、−90°〜45°、g = 9.8 | リアルタイムのゲージは 30% / 70% の 1RM、−90°〜60°（√3/2+1）、g = 9.80665、体重 65 kg |

本文の中の食い違い: 前腕の質量比（51 ページ 0.015M と 53 ページ 0.016M）、mx の定義（52 ページ「上位リンク群」と
53 ページの手首「手部だけ」）、比の式の分子に max が無い（53 ページ）、手首で rx = 0 だと閾値 Sc が 1 になる（§2-6）。

本文に無い限界: 慣性テンソルを回していない（§2-3）、リンク軸まわりの回転が取れない（§2-4）、加速度を 2 階差分で
求めている（§2-5）、左右差（本文は右だけ報告）、重力の向き、体幹の荷重、ω の 30 倍と unwrap 漏れ。

**受領CSVで確定（2026-09-23）**: `*gcvspl*cycle_work_shoulder.csv` の7名・右肘/右手首118行から、
中央値の平均0.9863102872（範囲0.3288428996〜1.7607681292）を再現した。第1サイクル除外後の0.88も再現。
分母 `one_rm` は14件すべてが次の旧ゲージ式と最大1.43e-14 Jの誤差内で一致した。

`E = r * 9.80665 * (0.42 * 65 * k + m_max) * (1 + sqrt(3)/2)`

肘はr=0.28・k=0.589・m_max=`elbow_R_outer`、手首はr=0.30・k=0.615・m_max=`wrist_R`。
本文53ページの16.73を用いる分母ではない。同梱 `out/03_reps_vs_score.md` はLPF系の式と混同している。
図13のスコアと図14の仕事量の点もこのCSVの平均に対応する。
ただし受領描画スクリプト・画像には別版があり、図の最終生成版は未確定。

**従来の照合の適用範囲**: 上表のフィルタ・30倍・unwrap・体幹荷重などの指摘は、調べたLPF系コードについてのもの。
今回判明したGCVSPL系の分子が全く同じ経路で作られたとはまだ確認していない。
ファイル名だけでGCVSPLの実使用を断定することも、LPF系の誤りを論文系列へそのまま当てはめることもできない。
元の「図14の数百Jも30倍と折り返しで説明できる」という記述は仮説に戻し、分子の生成経路の追跡を残す。
再現の根拠と図の版差は [Windows転送データの照合](docs/windows-transfer-audit-2026-09-23.md)。

あわせて: `config.EFFECTIVE_MASS_COEFFS` の「上肢」（0.276 + 0.19）は、実際には体幹（上胴体＋下胴体）の値。
追加対応（9a72f2c）でキーを `torso` に改名し、説明も修正した。係数・スコアの分母は変更していない。


### 6-7 スマホ1台＋PC内蔵カメラの入力（引き継ぎ追加、cc61163）

`NetworkMeasurement` は2視点のランドマークを受け取れるが、送信器はスマホか模擬端末のみだった。
`app.net.local_camera_sender` を追加し、PC内蔵カメラを `cam1` として同じ同期バッファへ送れるようにした。
カメラ取得・推論は専用スレッド、撮影時刻は画像取得直後のPC単調時計、画像サイズは取得画像の実寸。
模擬スマホ `cam0` とのペア生成、未検出時のスキップ、切断時のエラー、終了時の資源解放を自動テストした。

**未完了**: 実カメラの同期遅延・30 fps・Pixelとの校正、およびネットワーク計測のGUI／保存への接続。
`app.net.server` の単体起動は現在も受信診断用で、トルク保存やゲージ表示はしない。
起動と確認手順は `docs/phone_pc_capture.md`。

### 6-8 USB 経路のゲージの値 【修正済み 2026-09-23。閾値の式は USB は保留、混成は論文の式で決定 2026-09-24】

ゲージの手首はサイクルを検出するたびに 0 に戻るのに、肘は起動からの累積（`_continuous_energy_J`）で一度もリセットされなかった。
計測が長いほど肘の針は上がり続け、閾値の帯と比べる意味が無かった。サイクル確定の周回では、濾波したサイクルの仕事を一度
ゲージに入れるが、同じ周回の後段で累積の値に上書きしていた。また、ゲージの値はゲージのウィンドウがあるときしか計算せず、
閾値もゲージを作るときにしか計算しなかったので、HEADLESS では何も確かめられなかった。

**対応**: 肘の積算を `gauge_energy.ElbowGaugeEnergy` に移し、サイクル確定でリセットする（仕事だけ 0 に戻し、角度は残す）。
各部位の値は `gauge_energy.gauge_values`（どれも今のサイクルの正の仕事 [J]）。ゲージの有無によらず毎処理フレームで計算し、
終了時に `gauge_energy_<ts>_s2_g*.csv`（frame・cam_frame・t・cycle_index・4 部位）と同名の `.json`（閾値の帯、体重、r_x、
1RM の部位表）を書く。閾値はゲージの外で計算する（`GAUGE_AUTO_THRESHOLDS`）。`tests/test_gauge_energy.py`。

**保留**: 閾値の式は旧来のゲージの式 `E = r_x·g·(0.42·m1 + {0.3, 0.7}·m_max)·(√3/2+1)` のまま（手首の r_x = 0.30 m 固定、
体重 65 kg 固定）。§2-6 で直したスコアの分母とは別物。被験者 7 の再生では帯が約 95〜128 J、1 サイクルの正の仕事は 1〜12 J 程度で、
帯に一度も届かなかった。

### 6-9 USB 経路のサイクル検出が上下の動きを見ていない 【USB は判断待ち、混成は作り直した 2026-09-24】

`master_research_code.py` のサイクル検出（`utils.PushCycleDetector`、`mode='rise_to_rise'`）は、左肩の `RT_CYCLE_AXIS`
成分（既定 `y`）を見る。基準値は処理 5〜14 フレームの平均、「下がった」は基準から 1.5 cm 以上下か **1 フレームで 1 cm 以上**の
減少、「上がった」は基準 − 0.375 cm を下から横切ったとき。同じ軸は `RT_DYN_ON_RISE_ONLY`（立ち上がりまで τ = 0）の判定にも使う。

- 重力を体幹から推定するようになり（§1-5）、被験者 7 の再生では上が Z+ と出た。このとき `y` は奥行きで、プッシュアップの
  上下動（0.29 Hz、振幅 0.38〜0.40 m）は `z` に出る（`y` は 0.08〜0.14 m）
- 速さの閾値がフレームあたりなので、処理の間隔で効き方が 8 倍変わる。被験者 7 の再生では、4 Hz 間引きで 8 回検出したが、
  **間引きなし（GUI の既定）では 1 回も検出しなかった**。そのためゲージのリセットもサイクルの仕事の記録（`impulse_records`・
  `cycle_energy_debug`）も起きない

混成ステレオ（`app.runners.network_measure`）も同じ検出器・同じ既定（`cycle_axis="y"`、1 フレームあたりの速さ）を使う。

**決めること**: 見る軸を重力の推定結果（上の軸）にし、速さの閾値を秒あたり（m/s）に直すか。計測の挙動が変わるので、
直すなら被験者の録画の再生で検出の回数と位置を確かめてから。

### 6-10 実機の混成計測で 3D が壊れる（置き方） 【道具と手順を用意、置き直しは実機で】

2026-09-23 の実機の計測（Mac＋Pixel 7a、4.5 分、`~/Documents/WheelchairTorque/hybrid/measure/20260923_215653_236295`）を
`tools/verify_run.py check` にかけると、右手首のトルクの中央値が 71 N·m、最大 100 万 N·m 超、サイクルの仕事が最大 4 万 J だった。

- 原因は置き方。Mac と Pixel の間（基線）が 36.7 cm しかなく、肘での 2 本の視線のなす角が右 7.7°・左 16.4°。右腕では視線が
  ほぼ平行になり、2D のわずかな誤差で奥行きが大きく振れて、右上腕の長さが 20% の組で 0.12〜0.5 m の外（最大 4,734 m）に出た
- 左右の取り違えではない（外れた 685 組のどれも、片方のカメラの左右を入れ替えても直らない）。Pixel 側の線形補間でもない
  （Pixel の実際の撮影時刻で三角測量し直しても同じ割合で外れる）
- Mac の画面の下で手が切れていた（手首が画面内にある割合: 左 55%、右 74%）。MediaPipe は画面の外の点も visibility を高いまま
  外挿するので、visibility では弾けない
- Pixel は 14.3 fps、組が抜けた時間は 271 秒のうち 83 秒（Pixel の間隔が同期バッファの上限 100 ms を超える）
- 混成の経路には EKF も外れ値の除き方も無く、飛んだ 3D がそのままトルクになる（USB 経路は EKF のロバスト更新で吸収していた）

**対応（2026-09-23）**:
- `check` に配置と 3D の質の検査を足した: 長さが妥当な範囲に入る割合、前腕の長さのばらつき（`mobile/README.md` の目安 1.5 cm）、
  肘での視線のなす角（15° 以上）、肘・手首が各カメラの画面内にある割合。試し計測で置き直すべきかをその場で決められる
- Pixel のアプリに段階ごとの 1 秒あたりの数（カメラ・解析・推論・人・送信）と「30fps固定」を足した（`mobile/README.md`）。
  release 版を同じ鍵で作ってあり、`mobile/install.sh` で入れられる
- 当日の手順と記録シート: `docs/hybrid_field_run.md`（肘での視線の角を 15° 以上に。2 台を離せなければ被写体を近づける。被写体は 2 台の中間の正面。校正し直す）

**Pixel の速さ（2026-09-24、実機で段階の行を読んだ）**: 14.3 fps の主因は CPU での推論だった。「GPU推論」オフでは推論の段が
毎秒 16〜21（推論 80〜100 ms）、オンでは 29〜30（50 ms 台）。カメラは夜の室内灯でも 30fps固定の有無によらず約 30、解析・人・送信の段では
減らなかった。本計測は 30fps固定・GPU推論ともオン（`docs/hybrid_field_run.md` の表）。測定中に一度、Mac の無線が約 10 秒チャネルを離れて
WebSocket の ping が 15 秒返らず、Pixel がつなぎ直した（離れた理由は未特定）。前の計測の「組が抜けた 83 秒」にも同じ種類の途切れが
混じっている可能性がある。

**校正の位置関係（2026-09-24 に確認）**: `rot_trans_c1.dat` から、Pixel は Mac のカメラから見て右 18.4 cm・上 20.1 cm・後ろ 24.6 cm。
基線 36.7 cm のうち奥行きの分は視線の角にほとんど効かず、被写体が中間の正面 1.0 m でも 13.1°（横に 36.7 cm 並べれば 21°）。
`docs/hybrid_field_run.md` の表は横に並べた場合の値なので、2 台を同じ高さ・同じ奥行きで横に並べる。

**サイクルの仕事の dt（2026-09-24、未修正）**: `NetworkMeasurement._accumulate_cycle` はサイクル全体の仕事率の和に、
サイクルを確定したフレーム 1 つぶんの dt を掛けている（`np.sum(series) * dt`）。確定の直前で組が 0.2 s 抜けると、合成データで
肘の仕事が −0.48 J → +22.6 J、手首が 0.08 J → 11.9 J になった（検証は scratchpad の合成データ）。直すと既存の `cycle_work` の値が変わる。
また、上下だけの押し上げでは §6-9 のとおりサイクルを 1 回も検出せず `cycle_work` は空。検出されても値は 1 回ぶん（上げ＋下げ）の
符号付きの正味の仕事で約 0 J。静止と押し上げのトルク自体は、合成の体（実機の校正で 2D に落として三角測量から通した）で
手計算と 6.6×10⁻⁷ N·m 以内、押し上げの仕事は位置エネルギーの変化と 1% 以内で合った。

**混成の出力からスコアを出す道筋が無い（2026-09-24）**: 混成の計測は体重しか受け取らず、被験者番号も 1RM も使わない。
オフラインのスコア計算（`compute_cycle_energy_elbow_wrist.py`）は、`*_with_cycles.csv`・ランドマーク ID の列・全体座標の横長のトルクを前提にしていて、
混成の `kpts3d_*`（0〜15 の並び順の列）・`local_torque_*`（局所座標の縦長）を読めない。被験者番号もファイル名の日付から読んでしまう。
被験者 00 の 1RM は `m_max_part_00.json`・`m_max_all.csv` の 0 の行（肘 12.727・手首 8.235）と `m_max_all_merged.csv`（肘 outer 15・手首 inner 10）。
USB のゲージは肘 12.727、オフラインのスコアは肘 15 を使う（§6-4）。「0」と入れると USB 経路は既定のテンプレ（肘 6.0・手首 4.0）を使い、
配布版の .app は作業フォルダに `m_max_part_00.json` が無いので「00」でもテンプレになる。

**残り**: 実機で置き直して確かめる。置き方を直しても飛ぶ組が残るなら、骨の長さで外れ値を除く処理を足すか決める。

**USB 経路の機能の移植（2026-09-24 未明、ユーザーの指示で判断待ちを次のとおり決めた）**: 計画は
`~/.claude/plans/usb-1-2-3-4-5-6-7-8-11-12-13-14-zany-pnueli.md`。混成の子（`hybrid_measure`）に次を入れた。

- **ゲージの閾値（§6-8、混成）**: 論文 4.5.2 節・表 2 の負荷率 c=0.70・0.85 の理論仕事 W_c を、部位・左右ごとに
  `theoretical_1rm_work(joint, 体重, 実測の前腕長, c·1RM)` で出す（`app/gauge/thresholds.py`）。v<W_0.70 不足、W_0.70≤v<W_0.85 目標帯、
  v≥W_0.85 過負荷。1RM は `SUBJECT_ID`（"00"→0）で `m_max_all_merged.csv`（肘 `elbow_*_outer`・手首 `wrist_*`）を引く。表に無ければ帯を出さない。
  被験者 00・65 kg・前腕 0.25 m で肘 47.5〜56.9 J（W_1RM 66.3）、手首 9.6〜11.6 J（13.5）。USB の旧式の閾値はそのまま（保留）
- **ゲージの値**: 今の回の W_pos = Σmax(P,0)·dt（論文の定義）。子は `live.step()` の 1 周回（約 30 Hz）ごとに `@@GAUGE` の行（v2、
  `app/gauge/protocol.py`）を出す。回を閉じると now→prev、now=0
- **回の区切り（§6-9、混成）**: 左肩の奥行きの 1 フレームあたりの速さをやめ、肩の中点の重力の上向きへの射影（高さ）と m/s の速さで見る
  状態機械 `app/hybrid/rep_detector.py`。基準は先頭の窓（30 組）の中央値。関所（`HYBRID_DYN_GATE`、既定 1）を兼ね、閉じている間は
  トルクを記録するが仕事とゲージには積まない（`frames_` の `dyn_active`）。USB の検出器は変えていない（判断待ちのまま）
- **仕事の dt**: 1 フレームごとの P·dt、100 ms 超の抜けはまたがない（`app/hybrid/rep_work.py`）
- **EKF**: 混成にもつないだ（`app/hybrid/ekf.py`）。`HYBRID_EKF_PROFILE` が空なら同梱の既定値（GUI が渡す USB 向けの 1e-3 は、合成の
  押し上げで肘の W_pos を +52% にするため使わない）。格子の抜けは NaN の予測、0.5 s 超で作り直す。**注意**: 1.2 Hz で奥行きに速く揺れる体と
  Pixel 12 Hz の補間では、門が補間の点を外れ値と見て 20 cm 級の飛びを作った（EKF なし 最大 4.7 cm → あり 28 cm）。合成の押し上げ（Pixel 15〜30 Hz）
  では差が無い（最大 1.1 cm）。Pixel を GPU で 30 fps にし、朝の最初の計測の `kpts3d_raw_*` から `tune_ekf` でプロファイルを作るのが本筋
- **体格の検査**: プロファイルの有無によらず、先頭の窓の右肩–右肘の中央値が 0.10〜0.60 m の外なら終了コード 3。09-23 の記録を先頭から
  流すと 31 組目で「5.6 m」として止まる（前回は 4,734 m でも止まらなかった）
- **骨の長さの見張り（ゲージを守る）**: 腕の長さが先頭の窓の中央値から ±25% を超えたフレームは、その腕の仕事率を回の仕事とゲージに積まない
  （トルクは記録、`frames_` の `arm_ok_L/R`）。09-23 の記録の 20〜90 s では EKF ありでも右腕の |τy| が 95% で 9,000 N·m に跳ねた（置き方の失敗）。
  見張りを入れると同じ区間の左腕 74%・右腕 79% のフレームが積まれず、ゲージはほとんど動かない（合成の押し上げでは弾かれない）
- **重力**: 校正の最後に盤を立てて静止すると、短辺の向きを校正の meta に残し（`app/hybrid/gravity_board.py`）、計測が最寄りの軸に吸着して使う
  （符号は体幹で確かめる）。無ければ体幹から。meta の `gravity` に出どころ
- **出力**: `kpts3d_raw_<stamp>.csv`（EKF の手前、1/30 s 格子、抜けは NaN の行）、`aim_torque_vec_<stamp>_s2_g<重力>.csv`、`cycle_energy_`（肘の濾波 E±）、
  `gauge_energy_`、`cycle_work_` の末尾の列（W±・W_1RM・score）、meta の被験者・1RM・前腕長・帯・重力・EKF・処理時間。`hybrid-raw` の出力は
  `kpts3d_raw_<stamp>_retri*.csv` に改名（計測中の生 CSV を上書きしないため）
- **Mac のカメラ（13）**: OpenCV 5.0 の AVFoundation 経路は露出・WB・焦点を設定できない（`set` はすべて失敗、`get` は −1。実測 29.8 fps）。
  best effort で試して結果を meta の `mac_camera` に残すだけ（pyobjc は入れていない）
- **Mac の姿勢推定（14）**: `HYBRID_POSE_*` で受ける（GUI が渡す USB 向けの `POSE_ROI_ON=1`・`MP_INPUT_SCALE=0.5` を漏らさない）。既定は今と同じ
  （lite / VIDEO / 0.5 / 縮小 1.0 / ROI なし）。ROI の配線は未実装（`HYBRID_POSE_ROI=1` は警告して全画面）
- **USB の横の切り出しの不具合（確定、未修正）**: `master_research_code.py:2818` で切り出した画像の画素座標を、全体画像用の P0/P1（:2942）で
  三角測量している（x_start を戻していない）。論文のデータの作り方に影響するかは未確認
- **検証の道具**: `app/hybrid/replay.py`・`python -m app.runners.hybrid_replay <計測フォルダ>`（GUI は `HYBRID_REPLAY` を付けて起動）で記録を
  計測と同じ道筋で流し直す。`python -m tools.synth_session` で合成の押し上げの計測フォルダを作る。合成 13 cm×10 回で回 10、
  肘の W_pos 約 19.7 J（S≈0.30）・手首 約 6.9 J（S≈0.51）＝どちらも「不足」。**普通の押し上げでは帯に届かない見込み**（過去の実データの S と同じ桁）
- **W_pos は雑音で過大になる**: W_pos = Σmax(P,0)·dt は雑音を整流する（論文 5.4 の過大評価の機序）。合成の押し上げ 2 回・画素の雑音 0.3 px で、
  真値 49 J に対して EKF なし 149 J、EKF あり 72 J。実機の W_pos とスコアは雑音の分だけ過大に出うる
- **同梱の EKF が追える帯域は約 0.65 Hz**: 1 Hz・振幅 10 cm の動きでは頑健な門が追従を失い、1 次元の試算で 0.8 m ずれたままになる（押し上げの
  約 0.5 Hz は追える）。対策に発散の見張り（観測から 15 cm 以上のずれが 3 フレーム続いた点を観測で初期化し直す、meta の `ekf.divergence_resets`）を入れた
- 受信スレッドの `process` の時間: 合成で中央値 1.7〜5.8 ms、最大 17 ms（30 Hz の予算 33 ms に収まる）
- 残り: 実機での確認、ROI の配線、解析ページの EKF の説明文（`HYBRID_EKF_PROFILE` への書き換え）、オフライン用の書き出し

## 混成ステレオ経路の申し送り（2026-09-23）

- `app.hybrid` と `app.runners.hybrid_*` は Mac 内蔵カメラ＋Pixel 1 台用。T は cm、3D 出力は m。
  `calib.py` の m 出力とは混ぜない。USB 経路の単位修正・歪み補正は別作業。
- Android の時刻は analyze 開始、Mac は grab 直後であり、センサ露光時刻ではない。
  `--cam0-offset-ms` と生 2D の記録は実装済み。センサ timestamp / AVFoundation PTS の採用、
  エピポーラ誤差を使った ±100 ms の遅延走査は未実装。
- axis 重力では Mac カメラが水平である前提。天板を鉛直に固定する。
  床上の盤や Pixel 加速度による重力基準の決定は未実装。
- Android の自動再接続は実装済み（2026-09-24 に確認。`MainActivity.kt` の「自動の再接続」）。QR は最初の 1 回だけ読み、
  以後は覚えた接続先へ 3 秒ごとにつなぎ直す。PC 側は session を保存して使い回す（`app/hybrid/session.py`、作り直しは `--new-session`）。
  横向きの切断ボタン欠け、QR 案内文が解像度表示で上書きされる点は残る。
- 混成 3D の EKF・LPF、Qt 内への映像埋め込み、Mac の露出固定、配布・CI は対象外。
- phone-path の workdir/events/control と StopRequest の統合は別作業。
- release 鍵、`mobile/local.properties`、`keystore.properties`、`keystore/` は Git 管理外。
  同じ release 鍵を保ち、別 worktree では設定をコピーする。鍵のバックアップが必要。
- 実機の校正精度、表示負荷、計測精度はまだ未検証。実施手順と閾値は `mobile/README.md` を参照。
- 切断で受信統計がゼロに戻る既存不具合を修正。WebSocket 終了待ちは 0.5 秒に制限し、
  混成の writer はサーバ停止→drain→計算→close を同じ受信スレッドで実行する。
