# 要修正一覧

最終更新：2026-09-13 ／ 対象：このリポジトリ（旧 `MAINCODE/`）
検証の詳細は `力学計算_検証結果.md`（2026-09-02）と `力学計算_再検算_2026-09-08.md`。
EKF の自己チューニングは `docs/superpowers/specs/2026-09-08-ekf-self-tuning-design.md`。
修士論文本文側の修正は `修論_修正箇所リスト.md`（このリポジトリの外）。

§5-1〜§5-6 と §6 は、2026-09-09 の計画メモ「力学モデルの確定と、スコア再計算までの障害除去」（リポジトリ外）から
持ち越した項目（§1-5・§1-6・§5-7・§5-8 は 2026-09-13 の左右差の切り分けで見つかったもの）。
コミットメッセージが参照する番号（A-4、E-1、H-B など）を括弧で併記している。

優先度は **論文の数値に効くか** で並べている。§1 は結論そのものが動く。

---

## 残タスク（2026-09-13 時点）

行番号は `22459ce` 時点。§1-1〜§1-4 の行番号だけは修正前のもの。

### 判断待ち

| 項目 | 決めること | 効く先 |
|---|---|---|
| **§1-5** | 重力の向きをどう決めるか（体幹の向きから推定する／ファイルごとに指定する）。座標系が違う `5_1stereo_pose_scaled.csv` の扱い | **オフラインのトルクすべて**（スコアの分子） |
| §2-2 | 分母の等価質量に手部を含めるか（分子は前腕のみ） | スコアの分母 |
| §5-4（H-C） | ローカル座標系を作れないフレームで何を返すか（警告／NaN／現状維持） | スコアの仕事率 |
| H-A | 体幹の質量比。Winter（60 kg で 34.68 kg）を仮採用済み。リポジトリ系なら 32.82 kg | トルク 5.7% |
| §5-7 | リアルタイム経路の関節ラベルと基準点の対応をどう揃えるか（`wrist_R` が肘まわり） | リアルタイムの表示 |
| §4-1 | ラベル描画の既定をスプライトとネイティブのどちらにするか | 表示のみ |

H-B（関節角速度を相対量にする）と H-D（左右の符号条件）は 2026-09-13 に決着した（§5-2、§5-3）。

### データの所在確認

§5-1 の手首軸をスコアに入れるには、手の点（17〜20）の入った姿勢データが要る。

- `Adjusted 3D Pose/` の CSV 10 本はすべて 12 点で、手の列が無い
- 33 点が残っているのは被験者 2（`output_data/stereo_2_20250925_162436*.npy`、`(1217, 33, 3)`）だけ
- リポジトリ直下の動画 20 本は 2026-09-07〜11 の開発中の収録で、被験者の試技ではない

被験者 3・5・6・7・8 の 33 点データか元動画がリポジトリ外にあるかを確かめる。
無ければ、スコアの手首は現行の軸のまま出して限界として書く。

### 論文の数値に効く順

1. **§1-5** 重力の向きを直す（決め方は判断待ち）。直るまで、オフラインのトルクと §2-1 の数値はすべて無効
2. **判断**: §2-2・H-C・H-A。互いに独立なので並行して決められる。
   H-C の材料として、実データで §5-4 の早期リターンが何フレーム起きるかを先に数えておく（判断不要）
3. **データの所在確認**（上記）── 4 をスコア経路に入れられるかが決まる
4. **§5-1** 手首の基準軸を手のひら面にする。33 点が無ければスコア経路は見送り、リアルタイム側だけ入れる
5. **§5-4** 早期リターンのガード（H-C 待ち）
6. **§5-6** 出力に規約の版を刻む ── 1・4・5 と §5-2 でトルクと仕事率の意味が変わるので、再計算の前に入れる
7. **§6-1** スコアの通し再計算。**`--wrist-base` を付けること**（既定オフ。付けないと体幹荷重が入らない）。
   スコアはトルク CSV の全体座標の列から仕事率を求めるようになった（§5-2）
8. **本文の照合**: §1-4、R-5 の積分範囲（−90°〜45° に統一済み）が本文の定義と合うか、§2-3〜§2-5 の限界の記述。
   数値が確定してから

### リアルタイム表示に効く順（論文の数値には効かない）

1. **§5-7** 関節ラベルと基準点の対応を確認する（`wrist_R` が右肘まわりのトルクになっている）。
   仕事率の相対角速度の向き（§5-2）もこの対応に依存する
2. **§5-8** 体幹荷重のモデル（§2-1）をリアルタイム経路にも入れる（いまは 200〜300 N·m 出る）
3. **実機でまとめて行う**: §6-2 の実機確認、§3-2 の停止時 CSV、§6-3 の EKF 収録（S6）。
   どれも実機が要るので 1 回にまとめる。S6 の後に EKF の S9〜S11 が続く

### いつでもよい

§1-6（論文で使っていなければ）・§3-1（リポジトリ外）・§4-2・§4-4・§5-5

---

## 対応状況

| 項目 | 状態 | コミット |
|---|---|---|
| §1-1 角速度に fps を掛けている | **修正済み** | a44950d |
| §1-2 関節角を unwrap していない | **修正済み** | a44950d |
| §1-3 慣性回帰式に部位質量を渡している | **修正済み**（短いリンクで負になるガードも追加） | a44950d, 5336215 |
| §1-4 本文の I_xx と実測前腕長の食い違い | 未対応（本文側の判断が要る） | — |
| **§1-5 オフライン経路の重力が奥行き方向を向いている** | **未対応・最優先**（決め方は判断待ち） | — |
| §1-6 `compute_cycle_noise_contrib.py` に §1-1 が残っている | 未対応 | — |
| §2-1 体幹荷重がモデルに入っていない | **モデル確定・実装済み**。ただし報告値は §1-5 の誤りを含み無効（H-A は仮置き、スコア未再計算） | 6a9ffa2 |
| §2-2 分子と分母で等価質量の定義が違う | 未対応（判断待ち） | — |
| §2-3 慣性テンソルを回転させていない | 未対応（限界として明記する方針） | — |
| §2-4 2 点リンクは軸まわり回転を取れない | 限界として明記済み（テストにも記載） | — |
| §2-5 加速度がノイズを増幅する | 限界として明記済み | — |
| §3-1 壊れた別系統の出力 | リポジトリ内に対象なし（git 履歴にも無い） | — |
| §3-2 GUI から停止すると終了時の CSV が書かれない | 未対応（要実機確認） | — |
| §4-1 ネイティブ描画パスが既定で到達不能 | 未対応（判断待ち） | — |
| §4-2 ラベル描画の二重実装 | 一部解消（フォント取得だけ集約済み） | — |
| §4-3 `LandmarkEKF` の 2 実装と `h_fn` の無視 | **解消** | c792b41, feef20a, b3b76e1 |
| §4-4 その他（小） | 未対応 | — |
| §5-1 手首の基準軸が肘の屈曲軸のまま | 未対応 | — |
| §5-2 仕事率の ω が τ の軸と揃っていない | **修正済み**（相対角速度をトルクと同じ局所軸に射影） | 22459ce |
| §5-3 左右差 | **切り分け済み**。コードの非対称 3 件と、スコア経路の符号ずれを修正。残りはデータ由来 | 5ad3493, 22459ce |
| §5-4〜§5-6 | 未対応 | — |
| §5-7 リアルタイム経路の関節ラベルと基準点 | 未対応（意図の確認が要る） | — |
| §5-8 リアルタイム経路に体幹荷重のモデルが入っていない | 未対応 | — |
| §6-1 スコアの再計算 | 未実施 | — |
| §6-3 EKF の自己チューニング | S1〜S5・S7・S8 済み、S6・S9〜S11 が残り | d4b3a79〜93f1723 |
| 再検算 R-1〜R-7 | **すべて修正済み** | 4077b28, dc28a30, b7e308a, 5336215 |
| 計画 A-1（リンク定義の ID 化）・A-3（手の点の追加） | **修正済み** | e9fdca7, 8e20586 |
| 計画 B-1〜B-3（パイプラインの障害） | **修正済み** | 4b2a68e, 560ebd9 |

再検算の検算は `python tools/verify_dynamics_recheck.py`、
自動判定は `tests/test_dynamics_formulas.py` と `tests/test_keypoint_order.py`。

---

## §1 力学計算の誤り（最優先・数値で再現済み）

### 1-1 角速度に fps を余計に掛けている → スコアが正確に30倍 【修正済み 2026-09-08】

`compute_cycle_energy_elbow_wrist.py:229-230`

```python
elbow_omega = _gradient(elbow_angle, dt) * (args.fps if args.fps > 0 else DEFAULT_FPS)
```

`_gradient`（`:48-51`）は `np.gradient(series, dt)`。第2引数 dt はサンプル間隔なので
戻り値は既に rad/s。そこへ fps=30 を掛けている。

- 実測 ω：平均 **47.5 rad/s**、最大 **2827 rad/s**（450回転/秒）
- 修正後：平均 1.59 rad/s

**兄弟実装 `compute_elbow_cycle_work.py:100` は `gradient(ang, dt)` のみで正しい。**
同一プロジェクト内で2実装が30倍食い違っている。

**修正**：`* (args.fps ...)` を削除。

### 1-2 関節角を unwrap していない → ±π の折り返しが偽の巨大 ω を生む 【修正済み 2026-09-08】

`_angle_about_y`（`compute_cycle_energy_elbow_wrist.py:33-38`）は `arctan2` なので値域 ±π。
実データに **1試技あたり中央29回・最大58回**の折り返しがある。1回につき ω に 2π/dt = 188 rad/s の
スパイクが立ち、`W_pos = Σ max(τω,0)·Δt` は正側だけ拾うので必ず加算される（整流）。

**`compute_wrist_cycle_work_from_npy.py:87` は `np.unwrap(theta)` 済み。この1本だけ抜けている。**

**修正**：`np.gradient(np.unwrap(ang), dt)`。

### 1-1 + 1-2 を直したときのスコア

被験者3,5,6,7,8（m単位データ、両側）の `ratio_pos_vs_1rm`：

| | 平均 | 中央 |
|---|---|---|
| 修正前の実装 | **1.076** | 0.810 |
| 1-1 だけ修正 | 0.036 | 0.027 |
| 1-1 + 1-2 修正 | **0.004** | 0.003 |

修正前の値は本文の 0.99 および 0.33–1.76 を再現している。**本文の中心的数値が約1/350になる。**
ただしこれは §2-1 の体幹荷重を入れる前の値。

### 1-3 慣性モーメント回帰式に部位質量を渡している（正しくは体重）→ 慣性テンソルが負 【修正済み 2026-09-08】

`compute_torque_from_pose.py:326-328`

```python
mass = body_mass * seg.mass_fraction
inertia_tensors[idx] = calculate_inertia_tensor(seg.inertia_row, mass, length)
```

回帰式 `I = a·w + b·l + c`（`utils_dynamic.py:50-52`）の w は**全身体重**。

| | 修正前（部位質量） | 修正（体重60kg） | 教科書値 |
|---|---|---|---|
| 前腕 diag（L=0.191） | **[−0.00376, −0.00355, −0.00082]** | [0.00132, 0.00118, 0.00060] | — |
| 上腕 diag（L=0.299） | **[−0.00135, −0.00105, 0.00033]** | [0.00950, 0.00915, 0.00449] | — |
| 大腿 I_xx（L=0.42） | 0.0330 | **0.0969** | 0.110（Winter, k=0.323L） |

前腕は全軸で負。物理的にあり得ない。
**リアルタイム側 `master_research_code.py:3319-3339` は `w`（体重）を渡していて正しい。**
オフライン再計算側だけの誤り。

**修正**：`calculate_inertia_tensor(seg.inertia_row, body_mass, length)`。
影響は肘トルクで平均2.8%（慣性項の寄与自体が1.7〜4.6%）。

> **補足（2026-09-08）**: 体重を渡しても短いリンクでは負になる。体重 60 kg で
> 対角が正になる最小リンク長は 上腕 0.205 / 前腕 0.168 / 大腿 0.249 / 下腿 0.283 m で、
> 実測の下腿 0.232 m は範囲外。`calculate_inertia_tensor` に警告と一様棒近似への
> フォールバックを入れた（再検算 R-7）。

### 1-4 本文の I_xx = 74.33×10⁻⁴ は前腕長 0.30 m の値

前腕行に w=60, L=0.2999 で 74.33×10⁻⁴ に完全一致。ところが本文は実測前腕長を 0.19〜0.26 m と書いている。

| L [m] | 0.19 | 0.25 | 0.30 |
|---|---|---|---|
| I_xx [×10⁻⁴] | 12.6 | 46.3 | 74.4 |

定数項 c = −145.87 が b·l とほぼ相殺する領域なので L に極端に敏感。**要判断**。

実装はリンク長を先頭 30 フレームの中央値で決めるようになった（再検算 R-6、b7e308a）。
本文の値はこの長さとは別物なので、本文側で揃える。

### 1-5 オフライン経路の重力が奥行き方向を向いている 【未対応・最優先】

`compute_torque_from_pose.py:609` は重力を `[0, 0, −g]` に置く（外部荷重 `:646`、体幹荷重 `:698` も同じ向き）。
ところが入力の `Adjusted 3D Pose/*.csv` は**カメラ座標系で y が鉛直下向き**、z は奥行き。
途中の `filter_pose3d.py`・`recalc_elbow_local_torque.py` に軸の変換は無く、後段は y を鉛直として扱っている
（サイクル検出は右肩の y、`auto_detect_cycles_pose_ranges.py:23`）。

体幹の上向き（肩中点 − 腰中点）の単位ベクトル:

| ファイル | 上向き |
|---|---|
| 2・3_0・3_1・4_0・5・6・7・8・kpts3d_9 | [0.05〜0.15, **−0.97〜−0.99**, −0.09〜−0.19] |
| `5_1stereo_pose_scaled.csv` | [0.11, −0.31, **0.95**]（これだけ z が上。サイクル検出と集計に登場する） |

被験者 3（`3_0`）で重力の向きだけを変えた |τ| 中央値（N·m、`--wrist-base`）:

| | 手首 R | 肘 R | 肩 R | 手首 L | 肘 L | 肩 L |
|---|---|---|---|---|---|---|
| 現行 −z | 95.2 | 53.3 | 5.7 | 115.2 | 74.8 | 7.6 |
| +y（正しい向き） | 28.0 | 15.8 | 1.9 | 11.4 | 24.9 | 2.6 |

手首トルクは「体幹荷重 17.3 kg × g × 肩と手首の、重力に垂直な面内の距離」でほぼ説明できる。
その距離が現行では 0.52 / 0.63 m（腕の鉛直方向の長さ）、正しい向きでは 0.15 / 0.07 m。
**§2-1 の「肘が生理学的な目安 50〜80 N·m に収まる」は、この誤りの結果だった。**

オフライン経路のコード自体は左右対称（鏡映テストで誤差 0）。53.3 vs 74.8 の左右差は、体がカメラに対して
斜め（左肩 z = 4.03 m、右肩 z = 3.72 m）なことにこの誤りが重なって出ていた。

直し方は判断待ち: 体幹の向きから推定する（リアルタイム経路の `GRAVITY_AUTO_DETECT` と同じ考え方）か、
ファイルごとに指定するか。`utils.compute_local_torque` のフォールバック軸も全体座標の z を最初に使うので、
あわせて見直す。この行は git 履歴の最初（ca4e98d）からある。

### 1-6 `compute_cycle_noise_contrib.py` に §1-1 の誤りが残っている

`compute_cycle_noise_contrib.py:246-247` が角速度に fps を掛けている（§1-1 と同じく 30 倍）。
角度から求める ω（§5-2 で直した左右の符号ずれ）もそのまま。論文の「ノイズ整流」の議論に使っているなら直す。

---

## §2 モデルの妥当性（バグではないが主張に直結）

### 2-1 分子の力学モデルに「持ち上げている体重」が入っていない 【モデル確定・実装済み 2026-09-09】

修正前は `output_data/filtered_torque_lpf_recalc/` の実測トルクが **肘 0.50〜0.86 N·m／手首 0.82〜1.58 N·m** で、
計算していたのは**腕の分節を振る仕事**だった。§1 を直すとスコアが 0.004 まで落ちるのは本質的にこれが原因。

**確定したモデル**: 車椅子のアームレストを両手で押す動作を、手を外力との境界（固定端）とし、
そこから近位（前腕 → 上腕 → 肩）を持ち上げる逆順鎖として解く。肩より近位の体幹と頭は
「肩に載る外力」に集約し、脚は床／フットレストが支えるので含めない。分母は 1RM のまま変えない。

`compute_torque_from_pose.py --wrist-base` の持ち上げる質量の既定を「体重 − 両腕」（60 kg で 55.36 kg）から
「体幹＋頭」（34.68 kg）に変えた（6a9ffa2）。被験者 3 の |τ| 中央値（N·m、グローバルノルム）:

| | 手首 | 肘 | 肩 |
|---|---|---|---|
| 右 | 95.2 | 53.3 | 5.7 |
| 左 | 115.2 | 74.8 | 7.6 |

**この値は §1-5 の重力の向きの誤りを含んでおり無効。** 正しい向き（+y）では手首 28.0 / 11.4、
肘 15.8 / 24.9、肩 1.9 / 2.6 N·m（右 / 左）。

**残り**:

- `--wrist-base` は `store_true` で**既定オフ**（`compute_torque_from_pose.py:144-146`）。付けないと体幹荷重が入らない
- 体幹の質量比は Winter を仮採用（H-A）。`config.SEGMENT_MASS_FRACTIONS`。`--torso-mass` で上書きできる
- スコアは再計算していない（§6-1）

### 2-2 分子と分母で等価質量の定義が違う

| | 等価質量 m_x | 重心距離 r_g | 積 |
|---|---|---|---|
| 分母（理論1RM仕事量） | 前腕0.016M + 手部0.006M = **0.022M** | 0.586L | 0.01289 M·L |
| 分子（実測トルク・`RIGHT_SEGMENTS`） | **前腕0.016M のみ**（手部なし） | 0.430L | 0.00688 M·L |

重力項で分母が1.87倍過大。

場所は分母が `compute_cycle_energy_elbow_wrist.py:273-282`、分子が `compute_torque_from_pose.py:47-54`。
§2-1 で分子に体幹＋頭の荷重が加わったが、腕の分節の重力項としての食い違いはそのまま残っている。

### 2-3 慣性テンソルを回転させずグローバル系の ω に掛けている

`utils_dynamic.py:489`（numpy 経路）：`M = I @ dω + ω × (I @ ω)`。`I` は部位固定系の対角テンソル、
`ω` はグローバル系。`I_global = R I Rᵀ` の回転が無い。前腕の異方性は 2.2倍。
慣性項が全体の1.7〜4.6%なので最終影響は数%。

### 2-4 2点リンクの角速度は軸まわり回転を取れない（限界として明記）

`ω = (r × ṙ)/|r|²` はリンク軸まわりの回転（前腕の回内外）を常に0とする。
式の展開自体は正しい（`ω̇ = (r×r̈)/|r|² − 2(r·ṙ)(r×ṙ)/|r|⁴` を検算済み）。

### 2-5 加速度が位置の2階中心差分 → ノイズを約1800倍に増幅（限界として明記）

30 fps で dt² = 1.11×10⁻³。位置ノイズ1 mm が加速度 1.8 m/s²（g の18%）になる。

---

## §3 リポジトリの衛生と運用

### 3-1 壊れた別系統の出力が残っている 【リポジトリ外】

`output_data/torque/*_filtpos_est.csv` のトルクが異常：

| 被験者 | 肘 mean | 肘 max | 肩 max |
|---|---|---|---|
| 2 | 483 N·m | 2,465 | 5,106 |
| 3 | 664 N·m | **136,871** | **728,940** |
| 5 | 627 N·m | 34,022 | 32,101 |

生理学的上限（肘 50〜80 N·m）を桁で超えている。非m単位の位置を2階微分したためと見られる。
**スコア算出には使われていない**（実際の入力は `filtered_torque_lpf_recalc`、0.5〜1.6 N·m で妥当）が、
`out/00_README.md` が入力として挙げているので削除か明示的な隔離を推奨。

このリポジトリには `output_data/torque/` も `out/00_README.md` も無く、git 履歴にも無い。
修論フォルダ側で隔離する。

### 3-2 GUI から停止すると終了時の CSV が書かれない可能性が高い

`app/runners/worker.py:104-111` は「CSV の書き出しを待ちます」と表示してから `terminate()`（SIGTERM）を送るが、
`master_research_code.py` に SIGTERM ハンドラが無い。終了時にまとめて書く `kpts3d_*.csv` とトルク CSV は
書かれない可能性が高い。EKF 較正用の生 CSV は、これを避けるためにフレームごとに追記している
（`master_research_code.py:2839`）。

実機で確認し、必要ならハンドラでループを抜けるようにする。EKF とは独立に直すべき件。
EKF 設計メモの「スコープ外だが記録しておく別件」から移した。

---

## §4 コード品質（`/simplify` で見送った項目）

### 4-1 ネイティブ描画パスが既定で到達不能 ← 要判断

`master_research_code.py:3976` が `DRAW_LABEL_CACHE`（既定1）を
`:3992` の `USE_NATIVE_DRAW`（既定1）より先に判定するため、DLL 一括描画アームに入らない。
commit 18aadcc が意図せず殺した。**どちらを既定にするか決める必要がある**ため未変更。
ネイティブ側は `py_native_overlay` を import できたときだけ有効になる（`:107-110`）。

### 4-2 `_blit_label` / `_glyph_sprite` が `utils.put_text_jp` の二重実装 【一部解消】

`master_research_code.py:2538` 以降の `_label_font` / `_glyph_sprite` / `_blit_label` が、
`utils.put_text_jp`（`utils.py:196`）と同一シグネチャで文字描画を再実装している。

フォントの取得とキャッシュは `app.core.resources.japanese_font` に集約済みで、`_label_font` もそこへ委ねている。
残る重複は描画と折り返し。折り返し規則は乖離したまま
（`utils.py:226` は常に `textwrap.fill`、`master_research_code.py:2581` は `len(text) > line_width` のときだけ）。

**修正案**：`_glyph_sprite`/`_blit_label` を `utils.py` へ移し `put_text_jp` の内部実装にする。
`DRAW_LABEL_CACHE` フラグと両方の分岐が消え、`master_research_code_00.py:477-486` と
`utils.py:286-292` の呼び出しも高速化される。
`put_text_jp` の「新しい配列を返す」契約を保つ必要があるため中規模。

### 4-3 `LandmarkEKF` に2実装が恒久併存し、ベクトル化側が `cfg.h_fn` を黙って無視 【解消 2026-09-13】

- 逐次版とベクトル化版の一致を固定するテストを追加（`tests/test_landmark_ekf.py`、c792b41）。
  外れ値・欠測・dt 2 通りで相対差 1e-12 以内
- `extended_kalman_filter.py` へ移設（feef20a）
- `LandmarkEKF` に `h_fn` / `h_jac_fn` を渡すと生成時に例外（`extended_kalman_filter.py:248-252`）。
  `ExtendedKalman1D` で片側だけ渡すのも例外（b3b76e1）
- あわせて、NumPy 2 で逐次側（`EKF_VECTORIZED=0`）が `TypeError` で動いていなかったのを直した（b3b76e1）

一本化はしていない。系列別の雑音パラメータ（55511e1）も両実装に入れ、食い違いは一致テストで検出する形にした。

### 4-4 その他（小）

- `t_u_accum` / `t_u_fb` / `t_u_hist` / `t_u_cyc` / `t_u_gauge`（`master_research_code.py:3594-3945`）は、
  周囲の慣例（`t_seg` の使い回し）と不一致。区間は連続しているので統一可能
- `os.getenv` が `master_research_code.py` に 148 箇所。アプリ側は型付き設定（436d564）から環境変数を組み立てて
  渡すようになったが、スクリプト側の読み取りは従来のまま。`config.py` に `env_flag(name, default)` を置く余地がある

---

## §5 力学モデルの残件（計画メモから持ち越し）

### 5-1 手首の基準軸が肘の屈曲軸のまま（A-4 (1)）

リアルタイムは `links["wrist_R"]` が前腕、`parent_links["wrist_R"]` が上腕
（`master_research_code.py:3543-3576`）で、`y = 親 × z` は肘の屈曲軸になる。
手首のグローバルトルク 149 N·m のうち `wrist_R_local_y` は 6.4 N·m（4%）しか拾えていなかった。

オフラインはさらに悪い。手首基点の鎖の先頭 `forearm_R_wrist` は n=0 なので親が無く
（`compute_torque_from_pose.py:453-456`）、グローバル軸との外積で軸が決まる。
受け口の `root_parent_vec` はあるが `main()` から渡されていない。

手の点（17〜20）は 8e20586 で `pose_keypoints` に入ったが、まだどこからも使われていない。
人差し指と小指の中点を中手骨頭の代わりにし、「リンク＝手、親＝前腕」にする。
対象はリアルタイム、スマホ経路（`app/runners/network_measure.py` の `TORQUE_LINKS` / `PARENT_OF`）、オフラインの 3 か所。

**スコアに入れるには手の点の入った姿勢データが要る**（「データの所在確認」を参照）。

### 5-2 仕事率の ω が τ の軸と揃っていない（A-4 (2)、H-B） 【修正済み 2026-09-13】

修正前:

- **リアルタイム・スマホ経路**: `P = τ·ω`（全体座標の内積）で、ω は各部位の**絶対**角速度。
  肘角を保ったまま腕を振るだけで仕事が出ていた（合成データで 1 サイクル 7.6〜280 J）
- **スコア**: τ_y に、軸の作り方が別の角速度を掛けていた（肘は「+Y まわりの肘角」、手首は「水平面からの前腕の傾き」の微分）。
  鏡映すると τ と ω の片方だけが符号を変える。被験者 3 を左右反転すると、左の W_pos（301.94 J）が右の W_neg と一致した。
  **左右で逆の相（押し出しと戻し）を積算していた**

修正: 3 経路とも `utils.compute_joint_power` で `P = τ_y × (ω_リンク − ω_親)·y` を求める。
y 軸は局所トルクと同じ作り方（`compute_local_torque`）なので、鏡映で τ_y と ω_y が揃って符号を変え、P は変わらない。

| 経路 | リンク側 − 親 | 場所 |
|---|---|---|
| スコア（`--wrist-base` の鎖） | 手首: 前腕 − 手（固定）、肘: 上腕 − 前腕 | `compute_cycle_energy_elbow_wrist.py` の `_joint_powers` |
| リアルタイム・スマホ | `OMEGA_SOURCE` の部位 − `PARENT_OF` の関節の部位 | `master_research_code.py` の `parent_key_of`、`network_measure.py` の `_accumulate_power` |

スコアはトルク CSV の `*_local_y` 列ではなく、全体座標の列（`elbow_R_x` など）と姿勢から求めるようになった
（`recalc_elbow_local_torque.py` の局所列はスコアに使われない）。角度を経由しないので、
§1-1（fps の掛け戻し）・§1-2（折り返し）の誤りは構造的に起きない。

テスト: `tests/test_joint_power.py`（手計算の値と鏡映）、`tests/test_cycle_energy_angles.py`
（体幹を持ち上げたときの肘の正の仕事 = 位置エネルギーの増加 30.56 J、鏡映した動きで左右一致）、
`tests/test_network_measure.py::TestJointPower`（腕を一体で回しても肘の仕事は 0）。
`master_research_code.py` は import できないので、スマホ経路と同じ式で書いてある。

残り: 手首の局所軸はまだ全体座標の基準軸から作っている（§5-1）。リアルタイム経路は、
リンク側と親の対応そのものが関節ラベルと合っていない疑いがある（§5-7）。

### 5-3 左右差（E-1） 【切り分け済み 2026-09-13】

骨格を左右反転（鏡映）した動きを流し、元の右と反転後の左を比べた。コードが対称ならデータに依らず必ず一致するので、
コードが作る左右差とデータにある左右差を分けられる。

| 候補 | 結果 | 対応 |
|---|---|---|
| E-1a 左右で同じ符号条件 | **問題なし**。トルクも、親リンクとの外積で作る y 軸も擬ベクトルなので、`τ·y` は鏡映で変わらない。計画メモの「左右で逆符号が正常」はトルクを普通のベクトルとして扱った誤り | 変更なし（H-D は不要になった） |
| E-1b 右肩だけ上胴体の ω・ω̇ を反転 | **非対称**（手首・肘に約 1%）。`utils_dynamic.py` と `master_research_code.py` のネイティブ経路に複製されていた | 修正。補正を `utils_dynamic.trunk_segment_inputs` に一本化 |
| E-1c 肩の質量配分 A0/A1 | **問題なし**。右肩の分担 = 左手首までの距離 /（左＋右）はテコの分配として対称 | 変更なし |
| E-1d 慣性用のリンク長を左腕と右脚だけから取る | **非対称**（小） | 修正。腕と大腿の慣性テンソルを左右別に持つ |
| E-1f（新規）上胴体の関節位置 | **非対称**（肩トルクで 170%）。両チェーンとも `both_shoulder` の始点（左肩）まわりで計算していた | 修正。`calculate_individual_torques(p1s=...)` でチェーン側の肩・腰を渡す |
| スコア経路の τ と ω の軸の食い違い | **非対称**（左右で逆の相を積算） | 修正（§5-2） |

テストは `tests/test_left_right_symmetry.py`（鏡映で |τ| が入れ替わる、local_y の符号が保たれる、上胴体の M が左右で同じ）。

3 件を直しても、被験者 3 の実データの左右差はほぼ変わらない（スマホ経路で手首 |τ| 中央値 209 vs 304 N·m）。
**残る左右差はデータ由来**（体がカメラに対して斜め、など）。計画時の 71.8 vs 11.2 と 6a9ffa2 の 53.3 vs 74.8 は、
§1-5 の重力の向きの誤りも含んでいる。

### 5-4 ローカル座標系を作れないとグローバル値をそのまま返す（E-4、H-C）

`utils.compute_local_torque` の早期リターン（`utils.py:334, 337, 376, 381`）で、リンクが非 finite・退化・
参照軸が全滅したとき、`torque_global` がそのまま `_local_*` 列に出る。
100 N·m 級のグローバル値がローカル列に紛れてスコアに入り得る。
オフラインの `compute_torque_from_pose.py:456` と `recalc_elbow_local_torque.py:98`、
スコアの仕事率（`utils.compute_joint_power`）もこの関数で軸を作る。

### 5-5 定義が使われていない／出典が不明（E-3）

- `config.COM_FRACTIONS["hand"]`（0.506）と `config.m3`（手の質量）は参照ゼロ。§5-1 で使えるようになる
- 慣性係数 CSV の手の行（k=5）を渡している箇所が無い。手の慣性テンソルは一度も計算されていない
- 大腿の COM 比 0.433（`config.py:201`）の出典が未確認

### 5-6 出力ファイルに規約の版が無い（D-3）

2026-09-08 に左右ラベルが入れ替わり（R-1）、§5-1・§5-2 で `*_local_y` の意味も変わる。ところが
`forearm_R_{timestamp}.npy` などのファイル名にも、`compute_torque_from_pose.py` の `*_meta.json` にも規約の版が無い。
`config.py` に `LANDMARK_SCHEMA_VERSION` を置き、npy の名前と meta に入れる。既存ファイルは触らない（タグ無し＝v1）。

### 5-7 リアルタイム経路の関節ラベルと基準点が合っていない疑い

チェーンの部位は `[上腕, 前腕, 上胴体, 下胴体, 大腿]` の順で、関節トルクは部位 j の `p1`（リンクの始点）まわりに
j 以降の部位を足して求める（`utils_dynamic.calculate_individual_torques`）。`p1` は `part_calculations` の start なので:

| ラベル | 計算しているもの |
|---|---|
| `wrist_R` | 右**肘**まわり、全部位 |
| `elbow_R` | 右**手首**まわり、前腕・胴体・大腿（上腕を含まない） |
| `shoulder_R` | 右肩まわり（E-1f の修正後）、胴体・大腿 |

一方、局所軸（`links` / `parent_links`）と角速度（`omega_map`）はラベルどおり（`wrist_R` = 前腕リンク）に作っている。
意図した鎖の向きを確認し、部位の並びかラベルを揃える。§5-2 の相対角速度の向きもこれに依存する。
過去の検証（再検算 R-1）はリンクの左右ラベルを直したが、この対応は扱っていない。

### 5-8 リアルタイム経路に体幹荷重のモデル（§2-1）が入っていない

§2-1 のモデル（手を固定端に、肩へ体幹＋頭を載せる）はオフライン経路（`--wrist-base`）にだけ入っている。
リアルタイム経路とスマホ経路は下胴体（Imode 4）に体重 60 kg を丸ごと渡しており、被験者 3 の骨格で
手首・肘・肩の |τ| 中央値が 200〜300 N·m になる（生理学的な目安の 3〜4 倍）。

---

## §6 検証と運用

### 6-1 スコアの通し再計算（D-1）

中間データ（`output_data/filtered_pose_lpf` / `filtered_torque_lpf_recalc` / `m_max_all_merged.csv`）が
リポジトリに無いので、`Adjusted 3D Pose/*.csv` から回し直す。

```
Adjusted 3D Pose/*.csv
  → filter_pose3d.py                               *_lpf.csv
  → compute_torque_from_pose.py --wrist-base       トルク CSV
  → recalc_elbow_local_torque.py                   肘のローカルトルク
  → auto_detect_cycles_pose_ranges.py --pose-dir   *_with_cycles.csv
  → m_max_all_builder.py                           m_max_all_merged.csv
  → compute_cycle_energy_elbow_wrist.py            スコア
```

障害だった read-only ビューへの代入（B-1）、全 NaN 関節での Savitzky-Golay（B-2）、
サイクル検出の Windows パス（B-3）は修正済み。**§1-5（重力の向き）を先に直すこと。**
スコアは `--wrist-base` で作ったトルク CSV の全体座標の列を読む（§5-2）。
計画時の概算では、体幹荷重で肘トルクが約 49 倍になるのでスコアは 0.004 → 0.2 前後。手首も新たに寄与する。

### 6-2 実機確認（D-2）

R-1〜R-4・R-6 は表示に効く。USB 経路（`master_research_code.py`）とスマホ経路（`app/runners/network_measure.py`）の
両方で、ゲージの値が生理学的に妥当な範囲に入るか、16 点になった三角測量と EKF が 30 fps で回るかを見る。
§3-2 と §6-3 の S6 も同じ機会に行う。

### 6-3 EKF の自己チューニング

設計メモの S1〜S5・S7・S8 は実装済み（d4b3a79〜93f1723）。S8 までは実行時の既定の挙動を変えていない。
残りは **S6（実機での収録。間引きなしと 4Hz 間引きの 2 設定）** → S9（実行時への配線）→ S10（ロバスト更新）→ S11（GUI）。
同梱の既定値は S6 の実測が出るまで仮置き。詳細は設計メモの「実装順序」。
