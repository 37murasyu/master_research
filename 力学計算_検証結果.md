# 力学計算の妥当性検証

検証日：2026-09-02 ／ 対象：`MAINCODE/` の逆動力学〜仕事量スコア算出系
再現スクリプト：`scratchpad/verify_dyn.py`（慣性）、`verify_omega.py`・`verify_all.py`（角速度・スコア）

---

## 結論

**逆動力学の定式そのものは正しい。誤っているのは (a) 角速度の単位、(b) 角度の折り返し処理、(c) 慣性モーメント回帰式への引数、の3つ。**
このうち (a)(b) はスコアを **約350倍** 押し上げており、本文の中心的数値（スコア 0.99）に直接効く。

| | スコア `ratio_pos_vs_1rm` 平均 | 中央 |
|---|---|---|
| **現状の実装** | **1.076** | 0.810 |
| ω の ×fps だけ修正 | 0.036 | 0.027 |
| ω の ×fps + unwrap 修正 | **0.004** | 0.003 |

被験者3,5,6,7,8（m単位データ、両側）で算出。現状値は本文の 0.99 および 0.33–1.76 の範囲を再現している。

---

## A. 確定した誤り（数値で再現済み）

### ★A-1 角速度に fps を余計に掛けている → 全スコアが正確に30倍

`compute_cycle_energy_elbow_wrist.py:229-230`

```python
elbow_omega = _gradient(elbow_angle, dt) * (args.fps if args.fps > 0 else DEFAULT_FPS)
```

`_gradient` は `:48-51` で `np.gradient(series, dt)`。**第2引数 dt はサンプル間隔なので戻り値は既に rad/s**。
そこへ fps=30 を掛けているため ω が30倍になる。

- 実測 ω：平均 **47.5 rad/s**、最大 **2827 rad/s**（＝450回転/秒。肘関節では起こり得ない）
- 修正後：平均 1.59 rad/s、最大 94 rad/s

**兄弟実装は正しい**：`compute_elbow_cycle_work.py:100` は `ang_vel = gradient(ang, dt)` のみ。
つまり同一プロジェクト内で2つの実装が30倍食い違っている。

### ★A-2 関節角を unwrap していない → ±π の折り返しが偽の巨大 ω を生む

`_angle_about_y`（`:33-38`）は `arctan2` なので値域が ±π。実データには**1試技あたり中央29回・最大58回**の折り返しがある。
折り返し1回につき ω に 2π/dt = 188 rad/s のスパイクが立つ。

`W_pos = Σ max(τω, 0)·Δt` は**正の側だけを拾うので、符号がランダムなスパイクでも必ず加算される**（整流）。
A-1 を直したうえで unwrap を入れると、スコアはさらに **約8.6倍** 下がる（0.036 → 0.004）。

**ここでも別実装は処理済み**：`compute_wrist_cycle_work_from_npy.py:87` は `np.unwrap(theta)` を通している。この1本だけ抜けている。

### ★A-3 慣性モーメント回帰式に「部位質量」を渡している（正しくは体重）→ 慣性テンソルが負

`compute_torque_from_pose.py:326-328`

```python
mass = body_mass * seg.mass_fraction
inertia_tensors[idx] = calculate_inertia_tensor(seg.inertia_row, mass, length)
```

回帰式は `I = a·w + b·l + c`（`utils_dynamic.py:50-52`）で、**w は全身体重**。実測値で検算：

| | 現状（部位質量） | 修正（体重60kg） | 教科書値 |
|---|---|---|---|
| 前腕 diag（L=0.191） | **[−0.00376, −0.00355, −0.00082]** | [0.00132, 0.00118, 0.00060] | — |
| 上腕 diag（L=0.299） | **[−0.00135, −0.00105, 0.00033]** | [0.00950, 0.00915, 0.00449] | — |
| 大腿 I_xx（L=0.42） | 0.0330 | **0.0969** | 0.110（Winter, k=0.323L） |

**前腕は全軸で負**。物理的にあり得ない。体重を渡すと教科書値と一致する。

**リアルタイム側は正しい**：`master_research_code.py:3319-3339` は `w`（config.py の体重60kg）を渡している。
オフライン再計算側だけの誤り。

影響は小さい（肘トルクで平均2.8%、慣性項の寄与自体が1.7〜4.6%）が、負の慣性は審査で説明できない。

### ★A-4 本文の I_xx = 74.33×10⁻⁴ は前腕長 0.30 m のときの値

前腕行に w=60, L=0.2999 を入れると 74.33×10⁻⁴ で**完全一致**。導出はこれで確定。
ところが本文自身が実測前腕長を **0.19〜0.26 m** と書いている。その範囲では：

| L [m] | 0.19 | 0.25 | 0.30 |
|---|---|---|---|
| I_xx [×10⁻⁴] | 12.6 | 46.3 | 74.4 |

**最大6倍の開き**。定数項 c = −145.87 が b·l とほぼ相殺する領域なので、この回帰は L に極端に敏感。
本文の値と実測長のどちらを採るか要判断。

---

## B. モデルの妥当性（バグではないが、主張に直結する）

### ★B-1 分子の力学モデルに「持ち上げている体重」が入っていない ← 最重要

`filtered_torque_lpf_recalc` の実測トルク：**肘 0.50〜0.86 N·m／手首 0.82〜1.58 N·m**。

座位プッシュアップは体重を腕で持ち上げる動作なので、体幹荷重（60kg の大半）が肩→上腕→肘→前腕→手首と伝わる。
片側27kg・モーメント腕0.5mなら手首トルクは **10² N·m オーダー**になるはず。実測はその約1/100。

つまり現在計算しているのは**腕の分節を振るのに要する仕事**であって、プッシュアップの仕事ではない。
`--wrist-base` の逆順鎖（手首基点で前腕＋上腕を積む）は使われているが、体幹荷重を肩に載せる `--support-share` 経路
（`compute_torque_from_pose.py:665-696`）は本番データでは効いていない。

A-1・A-2 を修正するとスコアが 0.004 まで落ちるのは、**本質的にはこの欠落が原因**。
逆に言えば、これまで 0.99 に見えていたのは30倍と整流の誤差がたまたま埋め合わせていたため。

### ★B-2 分子と分母で等価質量の定義が違う

| | 等価質量 m_x | 重心距離 r_g | 積 |
|---|---|---|---|
| 分母（理論1RM仕事量） | 前腕0.016M + 手部0.006M = **0.022M** | 0.586L | 0.01289 M·L |
| 分子（実測トルク） | `RIGHT_SEGMENTS` は**前腕0.016Mのみ**（手部なし） | 0.430L | 0.00688 M·L |

重力項で**分母が1.87倍過大**。ダンベル項 m_max·r_x が支配的なので全体では数%だが、定義の不一致は残る。

### ★B-3 慣性テンソルを回転させずにグローバル系の ω に掛けている

`utils_dynamic.py:439-440`：`M = I @ dω + ω × (I @ ω)`。
`I` は部位固定系の対角テンソル、`ω`（`compute_segment_kinematics`）は**グローバル系**。`I_global = R I Rᵀ` の回転が無い。

前腕の異方性は横断 0.00132 に対し長軸 0.00060 で**2.2倍**。無視できる差ではない。
ただし慣性項自体が全トルクの1.7〜4.6%なので、最終影響は数%程度。

### ★B-4 2点リンクの角速度は軸まわり回転を原理的に取れない

`ω = (r × ṙ)/|r|²` は**リンク軸まわりの回転（前腕の回内外）を常に0とする**。
式の展開自体は正しい（`ω̇ = (r×r̈)/|r|² − 2(r·ṙ)(r×ṙ)/|r|⁴` は手計算と一致）が、
回内外を伴う動作では過小評価になる。制約として本文に明記が必要。

### ★B-5 加速度が位置の2階中心差分 → ノイズを約1800倍に増幅

30 fps で dt² = 1.11×10⁻³ なので、位置ノイズ1 mm が加速度 1.8 m/s²（g の18%）になる。
§5.4・§9.1 の「ノイズ整流」議論の実体はこれ。A-2 の折り返しスパイクと合わせて W_pos を押し上げる。

---

## C. 検証して「正しい」と確認できたもの

- **逆動力学の連鎖式**　`τ_j = Σ_{i≥j} M_i + Σ_{i≥j}(r_g,i − p_j)×F_i − τ_E − (r_x − p_j)×f_E`（`utils_dynamic.py:454`）── 標準形。`func_torque.py` とも整合
- **符号規約**　`F = m(a − g)` と外力側の `−(r_x−p_j)×f_E` は互いに整合。ダンベル `[0,0,−m·g]`、体幹荷重 `m·gravity` も同じ向き
- **角加速度の展開**　`ω̇ = (r×r̈)/|r|² − 2(r·ṙ)(r×ṙ)/|r|⁴` ── 手計算と一致
- **係数 16.73**　`(√2/2 + 1)·9.8 = 16.728`。∫_{−π/2}^{π/4} cos θ dθ = √2/2 + 1 = 1.7071 ── 導出も値も正しい
- **ローカル系への射影**　`compute_local_torque`（`utils.py:329`）── z=リンク方向、y=親×z、x=y×z の正規直交基底を作り `Rᵀ·τ` ── 正しい
- **正の仕事量**　`np.clip(power, 0, None)` ── 定義文の `max(τω,0)` と一致
- **慣性係数の行番号**　上腕=3、前腕=4 ── CSV の行順と一致
- **体節質量比**　前腕0.0160・上腕0.0227・手0.0060、COM比 0.430/0.436 ── Winter系と整合（上腕0.0227のみ出典要確認）
- **単位自動判定**　`_auto_pose_unit` は被験者2・9の非m単位データを cm と判定して正しく換算している（ただし座標中央値による heuristic なので原点位置に依存する脆さはある）

---

## D. 別系統の壊れた出力がリポジトリに残っている（要注意）

`output_data/torque/*_filtpos_est.csv` のトルクが異常：

| 被験者 | 肘 mean | 肘 max | 肩 max |
|---|---|---|---|
| 2 | 483 N·m | 2,465 | 5,106 |
| 3 | 664 N·m | **136,871** | **728,940** |
| 5 | 627 N·m | 34,022 | 32,101 |

生理学的上限（肘 50〜80 N·m）を桁で超えている。位置を非m単位のまま2階微分したためと見られる。
**これらはスコア算出には使われていない**（パイプラインの入力は `filtered_torque_lpf_recalc`、値は 0.5〜1.6 N·m で妥当）が、
`out/00_README.md` が `output_data/torque/` を入力として挙げているので、混同されないよう削除か明示的な隔離を推奨。

---

## E. 対応順

1. **A-1（×fps）を消す** ── 1行。ただしスコアが1/30になるので、本文の全数値が動く
2. **A-2（unwrap）を入れる** ── `np.unwrap(ang)` 1行。さらに1/8.6
3. **B-1 を決める** ── 体幹荷重を入れて「プッシュアップの仕事」を測るのか、
   現行どおり「腕分節の仕事」と定義し直して分母も揃えるのか。**ここが論文の主張そのもの**
4. A-3（体重を渡す）── 1行。影響は数%だが負の慣性は直すべき
5. A-4・B-2 ── 本文と実装で定義を1つに揃える
6. B-3・B-4・B-5 ── 限界として本文に明記（修正するなら B-3 は R の適用、B-5 は平滑化微分）
