# 被験者ゲージ（混成経路）設計書

- 日付: 2026-09-23
- 設計書の置き場: `murayama/fix-left-right-dynamics`（混成ステレオは `a29ee51` でこのブランチに取り込み済み）
- 実装のブランチ: `murayama/subject-gauge`（上のブランチの先端から分岐。作業ツリー `../master_research-gauge`）
- 状態: 作業者の指示により実装計画→実装へ進む（§9 の未確定事項は暫定の扱いを決めて進める）
- 画面案: `.superpowers/brainstorm/85004-1790167553/content/subject-screens-v4.html`（作業ツリー `master_research`。git 管理外）

## 1. 目的

座位プッシュアップの計測中に、**被験者が第 2 モニタで見るゲージ**を作り直し、実際の計測構成である
**混成ステレオ（Mac 内蔵カメラ＋Pixel 7a）の経路**に載せる。

現状の問題:

- 混成経路（`app/runners/hybrid_measure.py`）には被験者向けのゲージが無い。ゲージ（`Gauge_display.py`）は
  USB 2 台の旧経路（`master_research_code.py`）にしか無い。
- 旧ゲージは目標帯の範囲（J）を示さず、上限を超えた分を頭打ちで隠し、状態を色だけで示す。背景画像
  （`wheelchair_user.png`）はリポジトリに無い。

**成功の条件**: 第三者が一目で「どの部位が、今の 1 回で、目標帯に入っているか・不足か・過負荷か」を読める。
画面上のすべての表示が UI 部品選定規約・データ可視化選定規約の表の部品で作られている（§5.3）。

## 2. 作業者が決めたこと

| 項目 | 決定 |
|---|---|
| 見る人 | 被験者（ゲージ）。操作するのは作業者本人だけ（設定 GUI） |
| 被験者の課題 | 毎回の動作を**目標帯に収める**。上限超えは過負荷の警告 |
| ゲージの形 | 以前と同じ**扇形（半円のメーター）** |
| 地の色 | **暗い地（濃紺）** |
| 部位 | 左右の上腕（肘の仕事）・前腕（手首の仕事）の 4 つ。**肩は出さない** |
| 左右 | **鏡の対応**（被験者の右腕＝画面の右） |
| 追加表示 | **回数**（「N 回目」）と**前回の目盛り** |
| J の数値 | **スイッチで表示／非表示**。計測中もその場で反映し、値は保存して次回の初期値にする |
| 配線先 | **混成経路だけ**（USB 経路の旧ゲージは触らない） |
| 造形 | アプリアイコンと同じ造形言語（ピクトグラム、青・赤・琥珀）。見出しは青い帯（ポスター調） |
| 人物 | **頭から足先まで一続きに描く**（胴と脚を切り離さない。脊髄損傷者に対して不適切なため） |

## 3. 範囲

**含む**

- A: 造形の土台のうちゲージと計測画面が使う分（色の役割、ピクトグラム）
- B: ゲージ部品（描画・状態の決定・合成データのデモ）
- C: 混成経路への配線（計測側の集計、子→親の受け渡し、ゲージ窓の表示）
- 計測画面の変更のうち、ゲージの操作に要るもの（§5.2）

**含まない（別の設計書）**

- D: 設定 GUI 全体の作り直し（青い見出し帯・タブ化・キャリブレーション画面・解析画面）。
  `hybrid-stereo` の GUI 変更が main に入ってから行う。**それまで計測画面は今の骨格（左のナビ）のままで、
  画面案③の見出し帯とタブは D で入る。**
- USB 経路の旧ゲージの置き換え
- アプリアイコン（2026-09-23 に決定・生成・コミット済み: 濃紺の地に扇形ゲージ＋一続きの人物。§10）

## 4. 造形言語

1964 年東京・1972 年ミュンヘンの競技ピクトグラムの文法（均一な太さの線・丸い端・単色の面）。
ピクトグラムは本来「言葉が通じない来訪者にも一目で伝える」ためのもので、「第三者が一目でわかる」という要件と重なる
（経緯は一般的な記述に拠る。対外的に使うなら一次資料で確認）。

**色は 1 色に 1 つの役割だけ**を持たせる。色だけで状態を示さない（記号と文字を必ず添える）。

| 色 | 役割 | ゲージでの使い道 |
|---|---|---|
| 青 `#2563eb` 系 | 操作できるもの・現在地 | 見出し帯、目標帯（値が帯に入ると明るい `#60a5fa`） |
| 琥珀 `#fbbf24` | 動いているもの | 計測中の状態の点 |
| 赤 `#ef4444`（暗い地）／`#dc2626`（明るい地） | 異常・結果を壊すもの | 過負荷の弧と「✕ 過負荷」、異常終了 |
| 無彩色 | それ以外 | 地 `#0b1633`、溝 `#1e293b`、値の弧 白、補足文字 `#94a3b8` |

緑は使わない。コントラストは検証済み（濃紺の地に白 17.9:1、赤 4.7:1、補足文字 7.0:1）。
青い帯の上の赤い弧は 1.8:1 しかないので、値の弧の周りに地の色で 2px の縁を付けて分離する。

**人物のピクトグラム**（正面、座位プッシュアップの最上位）: 頭、肩、逆三角形の胴、両腕（アームレストを押す）、
膝（正面から見た太ももを横長の形で）、すね、両側の車輪＝赤いバンパープレート（キャンバー付き）、車軸＝スリーブ。
**胴・膝・すねは一続き**。座面は脚の奥を通し、膝と座面の隙間で「浮き」を示す。描画の順は
「椅子とプレート → 脚の切り欠き → 座面 → 身体」（切り欠きを身体の後に描くと胴に線が入る）。

## 5. 画面

### 5.1 ゲージ窓（被験者用）

設計上の座標は 800×450（16:9）。窓の大きさに合わせて縦横比を保って拡大し、余白は地の色で埋める。

**開く・閉じる**: 入力が Mac＋Pixel の計測を開始したときに開く（第 2 モニタがあればそこに全画面、無ければ
1280×720 の窓）。計測が終わっても閉じず、終了の状態を出したまま残す（被験者が最後の回の結果を見られるように）。
次の計測の開始で「Pixel 接続待ち」から描き直す。作業者が窓を閉じてもよく、次の計測の開始でまた開く。
ゲージ窓には操作部品を置かない（被験者が見るだけの画面）。

- **見出し帯**（青）: 人物ピクトグラム、「上肢の仕事量」、右端に状態（下表）。
- **中央**: 人物ピクトグラム。
- **扇 ×4**: 画面の左列が被験者の左腕、右列が右腕（鏡）。上段が上腕、下段が前腕。各扇は次で構成する:
  - 溝（地の弧、半円）
  - 値の弧: 0 J から左→右へ満ちる。**線形で、右端＝目標帯の上端 × 1.25**。超えた分は右端で止める
  - 目標帯: 溝の外側の細い弧。両端に J の数値（下端は数字だけ、上端に「J」）
  - 前回の目盛り: 直前に確定した 1 回の値の位置に、弧を横切る短い線
  - 中央に値（「132 J」）、その下に状態（「✓ 目標帯」／「✕ 過負荷」／不足は無表示）、その下に部位名（「左 上腕」）
- **凡例**: 「目標帯」「前回」の 2 つだけ。
- 弧の端は平ら（丸めると帯の境界と値の位置がずれる）。

**状態ごとの表示**（原因ごとに部品を分ける）

| 状態 | 見出しの右端 | 扇 |
|---|---|---|
| Pixel 接続待ち | スピナー＋「Pixel 接続待ち」 | 溝と目標帯だけ（値・前回なし） |
| 計測中・1 回目 | 「1 回目」 | 値あり、前回なし |
| 計測中・N 回目 | 「N 回目」（N＝完了した回数＋1） | 値と前回 |
| 正常終了 | 「✓ 終了 N 回」（N＝完了した回数） | 値なし、最後の回を前回の目盛りで残す |
| 異常終了 | 「✕ 異常終了」 | 直前の表示のまま |

**J の数値オフ**: 中央の値と目標帯の両端の数値を消し、状態（✓／✕）を中央に上げる。弧・目標帯・前回は同じ。
データ可視化規約 D4-03 は帯域の境界の定義を図の中で示すことを求める。オフは「被験者に数値を見せない」ための
意図的な例外で、オンに戻せば境界の値はいつでも見える。

**source=replay**: 行の `source`（§6.3）が `"replay"`（記録済み CSV の再生）のときは、局面（Pixel 接続待ち／
計測中／終了、上表）とは別に見出しの右端に「▶ 再生」の状態（R17-05 のピル）を添える。局面の表示自体は
局面ごとの表のとおりで、「▶ 再生」はそこに重ねる印だけ。`source="demo"`（合成データ）は表示には使わない
（`app/gauge/demo.py` を動かすためだけの印で、画面の出し分けには関わらない）。

### 5.2 計測画面の変更（作業者用、`app/shell/page_measure.py`）

骨格（左のナビ、`RunnerPage`）は変えない（D で変える）。変えるのは次だけ:

- **主ボタンを 1 つにする**: 停止中は「計測を開始」、実行中は「停止」に入れ替わる（R16-01）。
  理由を示さずに無効化していた「停止」ボタンを無くす（R15-04 の禁止事項の解消）。
- **「J の数値」スイッチ**（R1-01）を主ボタンの横に置く。入力が Mac＋Pixel のときだけ出す（R15-01）。
  切り替えるとその場でゲージ窓に反映し、設定 `GAUGE_SHOW_JOULES` に保存する。
- **見出しの状態**: 「● 計測中 N 回目」（琥珀の点）／停止中／✓ 正常終了／✕ 異常終了、と
  「● Pixel 接続」／「○ Pixel 接続待ち」（混成のときだけ）。点と文字の組（R17-06）。
- **設定を「実験者用の詳細設定」の開示にしまう**（R11-03）。中身:
  - 入力: 「USB カメラ 2 台／Mac＋Pixel」のラジオ（R2-01）。今のコンボボックスは n=2 で R2-03 の禁止事項
  - 被験者番号（R8-01）、体重（`BODY_MASS_KG` を表示に出す。欄の直後に「kg」＝R9-08）
  - 校正: 使う校正の日時（R13-05）と「変更」リンク（R16-10、キャリブレーション画面へ移る）
  - 既存の設定フォーム（開発・診断用は入れ子の開示）
  - 計算を壊す設定（`DEMO_MONO_GAUGE_ON` など既定を変えた 4 つ）が有効なら、開示の見出しに赤い件数バッジ（R20-03）
- **消すもの**: 画面上の説明文（「『デモ用』の項目は…」「出力先: …」「既定から変更: …」）。
  出力先は終了後の「出力フォルダ」リンク（R16-10）にする。
- ログは開示（R11-03、既定で開く）。未実行のときは「未実行」（R19-01）。

### 5.3 部品の対応

| 表示 | 規約の部品 |
|---|---|
| ゲージの扇 | R5-06 メーター（データ可視化 D4-03 の帯域の条件を満たす） |
| 目標帯の両端の J | D4-03 の帯域の定義 |
| ✓ 目標帯／✕ 過負荷 | R17-06（記号＋文字。色だけにしない） |
| 回数、✓ 終了、✕ 異常終了 | R17-05 状態 |
| Pixel 接続待ちのスピナー | R19-04 |
| 前回の目盛り・凡例 | 可視化の凡例（規約が許す読み方の補助） |
| 計測画面の各部 | §5.2 に記載 |

## 6. 構成

### 6.1 モジュール

| モジュール | 責務 | 依存 |
|---|---|---|
| `app/shell/theme.py` | 色の役割（上表）を名前付きの値として持つ。ゲージ用の暗い配色 | なし |
| `app/shell/pictograms.py` | 人物ピクトグラムの SVG を返す（大きさ・色を引数で） | なし |
| `app/gauge/model.py` | 純粋な計算: フレーム→弧の割合、状態（不足／目標帯／過負荷）、表示用の文字列、見出しの局面（状態機械） | protocol |
| `app/gauge/protocol.py` | ゲージの 1 行の書式（書く・読む）。§6.3 | なし |
| `app/gauge/scene.py` | 状態→場面: 800×450 の設計座標で弧・線・文字・スピナーを Qt に依存しない値として組み立てる | model, protocol, theme |
| `app/gauge/tracker.py`（**別セッションの持ち分**） | 計測側の集計: 今の回の正の仕事、前回、完了回数。排他ロック付きのスナップショット | protocol |
| `app/gauge/thresholds.py`（**別セッションの持ち分**） | 論文 4.5.2 節から W_0.70・W_0.85 を計算し、部位ごとの `band` を作る | compute_cycle_energy_elbow_wrist |
| `app/gauge/widget.py` | 描画（QPainter＋QtSvg）。`set_frame()` と `set_show_joules()`。動かない層の QPixmap キャッシュ（§6.6） | model, scene, theme, pictograms |
| `app/gauge/window.py` | ゲージ窓（第 2 モニタがあればそこに全画面、無ければ 1280×720 の窓） | widget |
| `app/gauge/demo.py` | 合成データで動かすデモ（`python -m app.gauge.demo`） | widget, window |
| `app/runners/network_measure.py`（変更・**別セッションの持ち分**） | 仕事率を積むところと周期の確定で `tracker` を呼ぶ。**既存の `cycle_work`・CSV は変えない** | tracker |
| `app/runners/hybrid_measure.py`（変更・**別セッションの持ち分**） | メインループ（Mac のカメラの約 30 Hz、間引かない）ごとにゲージの行を標準出力へ | protocol, tracker |
| `app/runners/worker.py`（変更） | 出力を行に分け、ゲージの行は `gauge_frame` シグナルへ、それ以外はログへ | protocol |
| `app/shell/controls.py` | `ToggleSwitch`・`Disclosure`・`CountBadge`・`StatusText`。§5.2 の部品 | theme |
| `app/shell/page_measure.py`（変更） | §5.2 | window, controls, theme |
| `app/core/settings.py`（変更） | `GAUGE_SHOW_JOULES`（真偽、既定オン）を追加。`BODY_MASS_KG` を表示に出す | なし |

`tracker.py`・`thresholds.py`・`network_measure.py`（変更分）・`hybrid_measure.py`（変更分）は別セッションの持ち分で、
この設計書のこのタスク（凍結の準備）では触らない。境界にあるのは `app/gauge/protocol.py` の行の書式（§6.3）で、
両セッションはこれだけを合意して独立に作業している。

### 6.2 データの流れ

```
子プロセス（--role hybrid_measure）                        GUI プロセス
  Pixel 受信スレッド                                       WorkerRunner
    NetworkMeasurement.process(pair)                         出力を行に分ける
      仕事率 P を求めるたび → tracker.add(part, P, dt)        ├ 普通の行 → ログ
      周期を確定したら     → tracker.close_rep()             └ 「@@GAUGE 」で始まる行 → decode → gauge_frame
  メインスレッド（live.step のループ、Mac のカメラの約 30 Hz。間引かない）  MeasurePage
    毎周 tracker.snapshot() と thresholds の band を合わせて              gauge_frame → GaugeWindow.set_frame
    protocol.encode(...) を書く（sys.stdout.write の 1 回で flush）      スイッチ → GaugeWindow.set_show_joules ＋ 設定に保存
                                                                        ワーカー終了 → 正常終了／異常終了の表示
```

ゲージ窓は **GUI プロセス**に置く。子プロセスのメインスレッドは OpenCV の窓を回しており、そこに Qt の窓を
混ぜずに済む。J のスイッチも同じプロセスの中で完結するので、プロセス間で切り替えを伝える仕組みは要らない。

### 6.3 ゲージの行の書式（v2）

1 行 = 接頭辞 `@@GAUGE ` ＋ JSON（UTF-8、改行で終わる）。書式は `app/gauge/protocol.py` が正本で、
子プロセス（別セッション）と GUI（このセッション）が合意した契約（constraints.md「行の形式 v2」）。

```json
{"v":2,"link":"waiting|connected","rep":6,"source":"measure|demo|replay",
 "parts":{"elbow_L":{"now":132.4,"prev":118.0,"band":[87.5,106.3],"w1rm":66.0},
          "elbow_R":{...},"wrist_L":{...},"wrist_R":{...}}}
```

- `rep` は完了した回数（0 以上の整数）。`prev` は完了した回が無ければ `null`。`now` は今の回の正の仕事 [J]。
- `band` は `[lo, hi]`（`lo < hi`）で、しきい値そのもの（§9）。子プロセス側で計算できない部位は `null`。
- `w1rm` はその部位の理論 1RM 仕事（参考値。今のゲージの描画は使わないが、あとで J 表示の分母などに使える
  余地として送ってもらっている）。
- `now`・`prev`・`w1rm` は数か `null`（送り主側で NaN は `null` にする）。
- `source` は誰が行を作ったかの印。`"measure"` は実計測、`"demo"` は合成データ（`app/gauge/demo.py`。表示には
  使わない）、`"replay"` は記録済み CSV の再生（§5.1「source=replay」）。
- 読む側（`app.gauge.protocol.decode`）は、接頭辞が無い行・JSON として壊れた行・`v` が 2 でない行を
  ゲージの行として扱わない（`None` を返し、呼び出し側はログに回す）。知らない鍵・`parts` の知らない部位名は無視する
  （版を上げずに送り主側だけ先に鍵を増やせるようにするため）。
- `encode` は小数 1 桁に丸め、JSON の区切りを詰め、ASCII にし、1 行を 512 バイト未満（macOS の `PIPE_BUF`。
  `QProcess` の `MergedChannels` で標準エラーと混ざらない）に収める。
- しきい値そのもの（`gauge_layout.json` の固定値）は使わない。GUI は行に載ってきた `band` をそのまま使う（§9）。

### 6.4 値の定義

- **今の回の正の仕事**: 周期が確定してからの Σ max(P, 0)·dt [J]（部位ごと）。負の仕事率は数えない。
  USB 経路の `gauge_energy.py`（別の作業中）と同じ「どの部位も今のサイクルの正の仕事」という考え方。
- **前回**: 直前に確定した回の値。**完了した回数**: `close_rep()` を呼んだ回数（既存の `cycle_count` とは別に数える。
  `cycle_count` は先頭の部位の記録件数で数えており、仕事率が空の回で数え漏れる）。
- **Pixel の接続**: Pixel の点が初めて届いて記録が始まったら `connected`（`MeasurementSession` の記録開始と同じ条件）。

### 6.5 スレッド

`tracker` は受信スレッドが書き、メインスレッドが読む。`add` / `close_rep` / `snapshot` は同じロックで守り、
`snapshot` は値のコピーを返す。

### 6.6 描画の速さ

ゲージの行は約 30 Hz で届く（§6.2）。GUI の描画がこれに追いつけないと、被験者が見る値が遅れて実際の動きと
ずれる。`app/gauge/widget.py` の `GaugeWidget` は、`scene.py` が返す場面の要素を役割（`role`）で 2 つに分ける:

- **動かない層**（地・見出し帯とアイコン・人物・溝・部位名・凡例・見出しのタイトル）: 窓の大きさが変わらない
  限り見た目が変わらないので、窓の大きさと `devicePixelRatio` の組ごとに 1 度だけ `QPixmap` へ描いてキャッシュする
  （`_ensure_static_pixmap`）。メモリを多く使ってでも、毎フレームの再構築を避けることを優先する。
- **動く層**（目標帯の色・値の弧とその縁・前回の目盛り・中央の値と状態の文字・帯の両端の数字・見出しの右端・
  Pixel 接続待ちのスピナー）だけを、キャッシュした `QPixmap` の上に毎フレーム重ねて描く。

`tests/test_gauge_widget.py::TestGaugeWidgetPerformance` がこの方針を確かめる: `test_static_layer_is_cached` は
同じ大きさでの再描画で静止層が作り直されないこと、`test_repaint_is_fast_enough` は 1920×1080 で 1 回の描画に
上限（15 ms）を設けて確かめる。実測では平均約 2.5 ms で、30 Hz（1 周期 約 33 ms）に十分な余裕がある。

行を読む側（`LineDemux`・`protocol.decode`）も 30 Hz で回るので、正規表現や大きなコピーを避けて軽く保つ
（`app/gauge/protocol.py` の実装方針）。`GaugeWidget.set_frame()` は状態を更新するだけで、Qt の `update()` を
呼んで再描画は Qt のイベントループにまとめて任せる（フレームを受け取るたびに強制的に repaint しない）。

## 7. エラーの扱い

- しきい値が計算できない（`band` が `null`。前腕長が人体の範囲外など、§9）: その部位は溝と部位名だけを描く
  （弧・状態・目標帯の数字は出さない。ただし J がオンで `now` があれば、中央の値の数字は出す）。計測は止めない。
- ゲージの行が壊れている: ログに回す（上記）。ゲージは直前の表示を保つ。
- 値が NaN・負: 0 として描く（正の仕事なので負にはならないが、防御として）。
- 第 2 モニタが無い: 通常の窓で出す。
- ワーカーが異常終了: 見出しに「✕ 異常終了」。値は直前のまま残す。

## 8. テスト（すべてテストを先に書く）

- `model`: フレーム→割合（0、帯の下端・上端ちょうど、上端×1.25 超えで 1 に頭打ち）、状態の判定
  （`now < lo` は不足＝文字なし、`lo ≤ now < hi` は「✓ 目標帯」、`now ≥ hi` は「✕ 過負荷」。下端は帯に含み
  上端は含まない）、見出しの局面の状態機械（Pixel 接続待ち／計測中／終了）
- `protocol`: 書いて読むと元に戻る（`band`・`w1rm`・`source` を含む）、接頭辞なし・壊れた JSON・版違い（`v` が
  2 でない）はゲージの行にならない、知らない鍵・知らない部位は無視する
- `scene`: 状態→場面が 800×450 の設計座標で組み立つ、`band` が `null` の部位は溝と部位名だけ（J オンなら中央の値の数字も）、`source=replay`
  で「▶ 再生」の要素が出る
- `tracker`（別セッションの持ち分）: 負の仕事率を数えない、`close_rep` で今→前回に移り 0 に戻る、回数、
  別スレッドから書きながら読んでも壊れない
- `thresholds`（別セッションの持ち分）: W_0.70・W_0.85 の計算、前腕長が人体の範囲外なら `band` を出さない
- `network_measure`（別セッションの持ち分）: 周期の確定で `tracker` が回を閉じる。**既存の `cycle_work_j` は
  変わらない**（回帰）
- `worker`: 塊で届いた出力を行に分け、ゲージの行はログに出ず `gauge_frame` に届く。行の途中で切れても次の塊とつなぐ
- `theme`: 文字と地のコントラストが基準以上（本文 4.5:1、大きい文字と図形 3:1）
- `widget`: offscreen で描画できる。状態ごとに期待する色の画素がある（過負荷で赤、帯の中で明るい青）。
  動かない層が窓の大きさが同じなら作り直されない、1920×1080 の 1 回の描画が上限（15 ms）に収まる（§6.6）
- `controls`: `ToggleSwitch`・`Disclosure`・`CountBadge`・`StatusText` の見た目と操作
- `page_measure`: 主ボタンが状態で入れ替わる、スイッチが混成のときだけ出る、スイッチで設定が変わる
- 目視: デモ（`python -m app.gauge.demo`）の画面を撮って、人物に切れ目が無いこと・文字が重ならないことを確認する

## 9. 未確定事項と暫定の扱い（実装は止めない）

- **しきい値**（解決済み）: `app/gauge/thresholds.py`（別セッションの持ち分）が論文 4.5.2 節の理論 1RM 仕事から
  部位ごとに W_0.70・W_0.85 を計算し、`@@GAUGE` 行の `band` として毎フレーム送る（§6.3）。GUI（このセッション）は
  `band` をそのまま使うだけで、`gauge_layout.json` の `energy_thresholds` の固定値は読まない
  （それを読むのは USB 経路の旧ゲージだけ。§3「含まない」）。前腕長が人体の範囲外など計算できないときは
  `band` が `null` になり、GUI はその部位を溝と部位名だけ（J オンなら中央の値の数字も）で描く（§7）。
- **計測画面の見た目**（返事なしのため設計どおり）: 今の骨格のまま中身だけ変え、見出し帯とタブは D で入れる。

**コードで決まったこと**: 体重の既定。GUI は `Settings.as_env` で全設定をワーカーへ明示して渡すので、GUI から計測するとき
実際に効いているのはスキーマの `BODY_MASS_KG` の 65 kg（`hybrid_measure` の 60 kg は GUI を通さない CLI 起動のときだけ）。
画面には効いている 65 kg を出す。動作は変えない。

## 10. 並行作業との関係

- 混成ステレオは `murayama/fix-left-right-dynamics` に取り込み済み（`a29ee51`）。その作業ツリー（`master_research`）では
  別セッションが `network_measure.py`・`hybrid_measure.py`・`app/hybrid/measurement.py`・`page_measure.py`・
  `settings.py` などを未コミットで編集している。実装はコミット済みの先端から切った別の作業ツリーで行い、
  元のブランチへの取り込みは、向こうの変更がコミットされてから載せ直して行う（向こうの作業ツリーには触らない）。
- `.app` の作業（`packaging/`、`app/core/workspace.py`）は `1aa6962` でコミット済み。
  アイコンはゲージ画面の縮図（濃紺の地、扇形ゲージ、今の印＝琥珀、前回の切れ目、一続きの人物）に決まり、
  `.app` にも反映済み。`app/shell/pictograms.py` の人物はアイコンと同じ形（胴・膝・すねが一続き、座面は奥）にする。
  両方が main に入った時点で、`packaging/icon.py` が `pictograms.py` の形を使うように寄せる（別の作業）。

## 11. この後

1. この設計書の確認
2. 実装計画（writing-plans）
3. 実装（テスト先行）→ デモで目視 → 実機（Pixel＋Mac）で確認
4. 別の設計書で D（設定 GUI 全体）
